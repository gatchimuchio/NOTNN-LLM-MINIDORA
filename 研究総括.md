# NOTNN-LLM-MINIDORA 研究総括

**基準日：2026-08-18**  
**対象：Layer-0／HDS／Kimi K3／Llama 3／DeepSeek V3.2／Qwen3.6／OpenAI・Anthropic公開構造**  
**現行状態：`LOCAL_MECHANISM / PRINCIPLE_CANDIDATE`**

## 1. 研究目的

本研究の目的は、LLMをTransformer、Attention、MoE、ニューラルネットワークといった特定実装へ固定せず、言語入力から文脈依存の結果形成までを閉じる機能責任として再記述し、非ニューラル計算機上へ再構成することである。

```text
ニューラルネットワーク   不使用
Transformer tensor       不使用
ベクトル埋め込み          不使用
GPU                       不要
外部MLフレームワーク      不要
```

構成した閉路は次の通り。

```text
自然言語入力
→ 言語アドレス化
→ 文脈状態
→ 内容依存参照
→ 直列変換・合成
→ 候補形成
→ HDS採否
→ 条件付き言語出力面
→ 自然言語出力
```

## 2. 中核理論

### 2.1 Layer-0 v4

```text
C = Execute(L₀, P, request, state, references)
```

- `L₀`：入力、状態、参照、命令形を結合し、変換・合成・結果形成・監査traceを実行する機構
- `P`：知識、規則、関係、状態遷移、変換手続、失敗条件を保持する命令形保持体

`P`はneural weightsに限定されず、symbolic graph、program、state machine、decision forest、table＋executor、hybridでよい。

Layer-0 v4の暫定責任は次の5項。

1. `LINGUISTIC_ADDRESSABILITY` — 言語単位を扱える
2. `CONTEXT_BOUND_STATE` — 現在文脈に結び付いた状態を持つ
3. `TRANSFORMATION_OR_COMPOSITION_CORE` — 参照・変換・合成を行う
4. `CONTEXT_DEPENDENT_RESULT_FORMATION` — 状態依存に結果候補を形成する
5. `RESULT_SURFACE` — 宣言された境界へ結果を返す

責任数と物理component数は同一ではない。一つの機構が複数責任を担ってよく、複数機構が一つの責任を共同で担ってよい。

### 2.2 LLM不可約機能論

- `F1`：内容依存情報検索
- `F2`：能力・命令形保持
- `F3-Q`：十分な直列深度
- `F3-D`：推論方向を規定するoperator inventory

計算回数を増やすことはF3-Qを増やすが、存在しないoperatorは生まれない。推論方向を変えるには命令形保持体を変更する必要がある。

### 2.3 HDS

HDSは生成と採否を分離し、証拠、矛盾、未知、境界、権限、失敗、再開条件を保持する。

```text
証拠あり・矛盾なし → PASS / 合格
証拠不足・未知      → SUSPEND / 保留
責任反証・出力遮断  → FAIL / 失敗
対象外              → NOT_APPLICABLE / 非適用
```

HDS Gateだけが最終出力と記憶書込みの権限を持つ。

## 3. 各モデルの構造抽出

| 系統 | 構造の最上位 |
|---|---|
| K3 | 時間・文脈・深さ・幅・計算予算を分離して選ぶ多軸選択的情報流 |
| Llama 3 | 一つの共有Dense bodyとresidual streamを全入力が通る連続変換 |
| DeepSeek V3.2 | 文脈位置top-k選択＋潜在KV圧縮＋MoE＋MTP |
| Qwen3.6 | Gated DeltaNet×3＋Gated Attention×1。MoE／Denseを独立に交換可能 |
| OpenAI公開system | 外部routerとreasoning effortによる計算予算選択 |
| Anthropic公開system | 同一model内adaptive thinkingとeffort制御 |

## 4. K3の普遍性と局所性

比較後に抽出されたK3の上位原理は、単なる「選択」ではない。

```text
K3の上位原理
= 多軸選択的情報流

何を保持するか
何を読むか
どの深さを再利用するか
どの専門能力を使うか
どれだけ計算するか
を独立軸として入力依存に選択する
```

### 普遍候補

- 入力依存で情報・状態・処理・計算量の寄与を変える
- 状態を継承する
- 直列変換を重ねる
- 必要計算量を問題に応じて変える

### 複数系統に共通

- Gated Delta系有限状態＋周期的global attention：K3／Qwen
- routed expert＋shared expert：K3／DeepSeek／Qwen MoE
- latent context compression：K3／DeepSeek
- MTP：DeepSeek／Qwen
- effort制御：K3／Qwen／OpenAI／Anthropic公開system

### K3固有性が強い構造

1. **Attention Residuals** — 過去のどの深さを使うかを入力依存に選択
2. **KDAの細粒度forget gate** — Gated DeltaNetの親原理をchannel-wiseに精密化
3. **Stable LatentMoEの具体構成** — 896 expertからtop16＋shared2
4. **選択軸の多重化** — 時間・文脈・深さ・幅・予算を同時制御

現時点で最もK3固有性が高いのはAttention Residualsである。

## 5. K3とLlama 3の差分

```text
K3      = 何を動かすかを選ぶ制御型
Llama 3 = 一つの共有bodyを全入力が全部通るDense連続構成型
```

| 軸 | K3 | Llama 3 |
|---|---|---|
| 状態 | `RETAIN / DELTA_WRITE` | append-only KV ledger |
| 深さ | AttnResで過去段階を選択 | residual streamを順次継承 |
| 幅 | top-k experts | Dense FFN全通過 |
| 文脈 | KDA＋周期的Gated MLA | 各blockのGQA |
| 計算量 | low／high／max可変 | profileごとに固定 |
| 一貫性 | active subgraphが変化 | 同一parameter bodyを共有 |

Dense shared bodyと主体的一貫性の関係は構造寄与仮説であり、因果確定ではない。

## 6. DeepSeekとQwenによる相対化

### DeepSeek V3.2

DeepSeekは長文脈問題に対して、全情報を有限状態へ圧縮するのではなく、読む文脈位置をtop-kで先に選ぶ別解を示す。

```text
K3／Qwen
全体を有限状態へ圧縮
→ 周期的global attentionで補正

DeepSeek
文脈を保持
→ 読む位置を選択
```

したがってKDAはlong-contextの普遍解ではない。普遍側は「全情報へ毎回同じ計算をかけない」という原理である。

### Qwen3.6

Qwen3.6はMoE版とDense版の双方に、Gated DeltaNet×3＋Gated Attention×1を採用する。

これにより次を確認した。

```text
hybrid attention
≠ MoE固有

attention構造とFFNのDense／MoEは独立軸
```

K3の69 KDA＋24 Gated MLAとQwenの3:1構造はほぼ同じ比率へ収束している。ただしKDAの具体式はK3局所である。

## 7. 非ニューラル参照実装

実装した参照系統：

- K3型
- Llama 3型
- DeepSeek V3.2型
- Qwen3.6 MoE型
- Qwen3.6 Dense型

同一toy課題では5系統が同じstatus・answerを返した。これは実モデル同等性ではなく、異なるcompute topologyが同じ局所Layer-0機能を実装可能であることを示す。

## 8. 暫定統合アーキテクチャ

```text
Dense constitutional core
+ sparse peripheral specialists
+ multi-axis selective memory/context
+ independent HDS authority gate
```

### Dense constitutional core

- identity／continuity
- 基本判断規則
- 会話方針
- 共通residual state
- 全入力へ適用するconstitutional constraints

### Sparse peripheral specialists

- 検索
- code
- tool操作
- domain固有処理
- 高コスト推論

### Multi-axis selective layer

- 時間：何を保持・更新するか
- 文脈：どこを読むか
- 深さ：どの過去段階を再利用するか
- 幅：どの専門処理を起動するか
- 予算：どれだけ計算するか

### HDS authority gate

- evidence
- contradiction
- unknown
- risk
- boundary
- memory write
- final emission

## 9. 検証結果

```text
K3完全版                       20/20
Llama 3完全版                  23/23
DeepSeek V3.2参照版             7/7
Qwen3.6 MoE／Dense参照版       10/10
Layer-0責任                    5/5
negative controls              8/8
5モデル同一toy課題             5/5一致
```

確認済み：

- NN、Transformer library、vector embedding、GPUなしの閉じたtext path
- 異なる公開architectureの非ニューラル機能射影
- unknown／contradictionの`SUSPEND`
- result surface遮断の`FAIL`
- 生成と採否権限の分離

## 10. 未成立・未主張

- 各実モデルのweight、知識量、性能、速度の再現
- frontier benchmark同等性
- open-domain汎用LLM完成
- multimodal semantics
- Dense構造が主体性を生むという因果証明
- 市場用語としてのLLM最終認定
- 唯一最小構成または普遍原理の確定

## 11. 現在の結論

```text
非ニューラル閉路の局所成立          = 確認済み
公開構造の機能射影                  = 確認済み
K3の普遍／共通／局所の分離          = 暫定成立
統合アーキテクチャ                  = PRINCIPLE_CANDIDATE
一般open-domain LLM完成             = 未成立
唯一最小構成・普遍原理              = OPEN
```

本成果の意味は「ニューラルネットワークを使わないチャットボットを作った」ことではない。異なるfrontier系統を同じ機能座標へ写像し、それぞれの構造的特徴を残したまま非ニューラル計算へ再構成できたことにある。
