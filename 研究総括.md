# NOTNN-LLM-MINIDORA 研穵総括

**基準日：2026-08-18**  
**対象：Layer-0／HDS／K3／Llama 3／DeepSeek V3.2／Qwen3.6／OpenAI・Anthropic公開構造**

# 非ニューラルネットワークLLM研究：全体成果概要

**基準日：2026-08-18**  
**現行状態：`LOCAL_MECHANISM / PRINCIPLE_CANDIDATE`**

## 1. ここまでに成立したこと

本研究では、LLMを特定のニューラルネットワーク部品ではなく、言語入力から文脈依存の結果形成までを担う実行責任として再記述した。

その上で、Kimi K3、Llama 3、DeepSeek V3.2、Qwen3.6の公開構造をHDSで分解し、各構造の情報流をニューラル演算なしの参照実装へ再投影した。

```text
ニューラルネットワーク   不使用
Transformer tensor       不使用
ベクトル埋め込み          不使用
GPU                       不要
外部MLフレームワーク      不要
```

同時に、以下の閉じた経路を構成した。

```text
自然言語入力
→ 言語アドレス化
→ 文脈状態
→ 内容依存参照
→ 直列変換・合成
→ 候補形成
→ HDS採否
→ 条件付き言語出力面
→ 自然言語出力
```

## 2. 中核理論

### 2.1 Layer-0 v4

```text
C = Execute(L₀, P, request, state, references)
```

- `L₀`：入力、状態、参照、命令形を結合し、変換・合成・結果形成・監査traceを実行する機構
- `P`：知識、規則、関係、状態遷移、変換手続、失敗条件を保持する命令形保持体

`P`はneural weightsに限定されず、symbolic graph、program、state machine、decision forest、table＋executor、hybridでよい。

### 2.2 LLM不可約機能

- `F1`：内容依存情報検索
- `F2`：能力・命令形保持
- `F3-Q`：十分な直列深度
- `F3-D`：推論方向を規定するoperator inventory

### 2.3 HDS

HDSは生成と採否を分離し、証拠、矛盾、未知、権限、失敗、再開条件を保持する。

```text
証拠あり・矛盾なし㢆� PASS / 合格
証拠不足・未知      → SUSPEND / 保留
責任反証・出力遮断  → FAIL / 失敗
対象外              → NOT_APPLICABLE / 非適用
```

## 3. 各モデルから抽出した構造

| 系統 | 構造の最上位 |
|---|---|
| K3 | 時間・文脈・深さ・幅・計算予算を分離して選ぶ多軸選択的情報流 |
| Llama 3 | 一つの共有Dense bodyとresidual streamを全入力が通る連続変換 |
| DeepSeek V3.2 | 文脈位置のtop-k選択＋潜在KV圧縮＋MoE＋MTP |
| Qwen3.6 | Gated DeltaNet×3＋Gated Attention×1。MoE/Denseを独立に交換可能性 |
| OpenAI公開system | 外部routerとreasoning effortによる計算予算選択 |
| Anthropic公開system | 同一model内adaptive thinkingとeffort制御 |

## 4. K3の相対化

比較後に残ったK3の上位原理は、単なる「選択」ではない。

```text
K3の上位原理
= 多軸選択的情報流
= 何を保持するか
  何を参照するか
  どの深さを使うか
  どの能力を使うか
  どれだけ計算するか
  を分離して入力依存に選ぶ
```

選択自体はDeepSeek、Qwen、Llama、OpenAI、Anthropicにも別形態で現れる。

K3の固有性が最も強いのは、現比較では以下。

1. `Attention Residuals`：深さ方向の内容依存選択
2. `KDA`のchannel-wise細粒度forget gate
3. 896 routed / top16＋2 sharedというStable LatentMoE具体構成
4. 選択を5軸へ重ねた構造全体

## 5. 構成的検証

- K3完全版：20試験合格、Layer-0責任5/5、negative control 8/8
- Llama 3完全版：23試験合格、self-test 10/10、Layer-0責任5/5、negative control 8/8
- DeepSeek V3.2参照版：self-test 7/7
- Qwen3.6参照版：MoE/Dense合計10/10
- 5モデル同一toy課題：5/5で状態と答えが一致

これは実モデル同等性能を証明しない。異なる内部topologyで同じ局所機能を成立させられることを示す。

## 6. 現在の本命設計

```text
Dense constitutional core
+ sparse peripheral specialists
+ independent HDS authority gate
```

- 主体・基本判断・継続性：Dense core
- 検索・code・tool・domain処理：Sparse specialists
- 証拠・矛盾・未知・最終出力権限：HDS gate

これは現時点の設計候補であり、最終原理ではない。

## 7. 未成立

- K3、Llama 3、DeepSeek、Qwenのweight・性能・知識量の再現
- open-domain自然言語能力
- frontier benchmark同等性
- multimodal semantics
- Dense構造が主体性を生むという因果証明
- 市場用語としての「LLM」最終認定


---

# HDSとLLM不可約機能の統合

## 1. 対象・目的・手段

- 対象：言語入力から結果出力までを閉じる計算システム
- 目的：ニューラルネットワークを必要条件にせず、LLM的機能を再構成する
- 手段：Layer-0、F1/F2/F3、HDS Gateを接続する

## 2. Layer-0

```text
C = L₀ ⊗ P
C := Execute(L₀, P, request, state, references)
```

### `L₀`：実行責任

- requestとsystem boundaryを受理する
- state/contextを結合する
- `P`と参照資源から必要部分を選ぶ
- 変換・合成・候補形成を実行する
- 結果面とtraceを返す
- 失敗、未知、未閉包を返す

### `P`：命令形保持体

- 言語・記号の処理規則
- 知識・関係
- 状態遷移
- 推論operator
- 候補選択補助
- 結果実現規則
- 適用境界・失敗条件

`P`の基盤素材は適合条件ではない。

## 3. F1 / F2 / F3

### F1：内容依存情報検索

位置や固定順序だけでなく、現在の入力・目的・文脈に応じて参照対象を変える。

実装例：

- BM25
- typed fact query
- symbolic DSA top-k
- stage content selection
- attention相当の重み付け

### F2：能力・命令形保持

能力は世界事実と分離して保持する。

```text
能力：grammar / rule / operator / program
事実：document / record / relation / observation
```

この分離により、能力を変えずに事実を交換し、事実を維持したまま推論方向を変更できる。

### F3-Q：直列深度

複数段の変換結果を次段へ渡し、単発lookupでは出ない結果を形成する。

### F3-D：推論方向性

どの推論が可能かはoperator inventoryで決まる。

```text
計算回数を増やす → 既存operatorを深く回す
operatorを変える → 推論方向そのものを変える
```

## 4. HDS Gate

HDSは生成器から採否権限を分離する。

主な検査：

- Evidence Gate：出典・proofがあるか
- Contradiction Gate：反対事実が未解消でないか
- Unknown Gate：未観測を推測で埋めていないか
- Boundary Gate：対象範囲が明示されているか
- Authority Gate：出力・memory書込み権限を誰が持つか
- Reopen Gate：新事実で再開できるか

## 5. 判定

| 状態 | 意味 |
|---|---|
| PASS / 合格 | 必須責任と証拠が確認済み |
| FAIL / 失敗 | 必須責任またはnegative controlが反証済み |
| SUSPEND / 保留 | 証拠不足、未知、矛盾、境界不明 |
| NOT_APPLICABLE / 非適用 | 宣言scope外 |

## 6. 主張境界

この統合が直接示すのは、candidate-localな構成可能性である。

```text
非ニューラル閉路の成立       = 構成的に確認
各公開モデルの機能射影       = 局所機構として確認
実モデル同等性能             = 未確認
普遍的一意最小性             = 未主張
```


---

# 非ニューラルLLM統合アーキテクチャ案

## 0. Status

```text
STATE = PRINCIPLE_CANDIDATE
EVIDENCE = K3 / Llama3 / DeepSeek / Qwen の局所実装比較
UNIVERSALITY = OPEN
```

## 1. 暫定本命

```text
Dense constitutional core
+ sparse peripheral specialists
+ multi-axis selective memory/context
+ independent HDS authority gate
```

## 2. 責任分離

### Dense constitutional core

- identity / continuity
- 基本判断規則
- 会話方針
- 共有residual state
- 全入力へ適用するconstitutional constraints

### Sparse peripheral specialists

- 検索
- code
- tool操作
- domain固有処理
- high-cost reasoning

### Multi-axis selective layer

- 時間：何を保持・更新するか
- 文脈：どこを読むか
- 深さ：どの過去段階を再利用するか
- 幅：どの専門処理を発火するか
- 予算：どれだけ計算するか

### HDS authority gate

- evidence
- contradiction
- unknown
- risk
- boundary
- memory write
- final emission

## 3. 各系統から採用する候補

| 出自 | 採用候補 |
|---|---|
| K3 | AttnRes、KDA細粒度更新、多軸選択、effort制御 |
| Llama 3 | Dense shared body、causal continuity、全体policyの一貫性 |
| DeepSeek | 文脈位置top-k、latent context compression、MTP |
| Qwen | 3:1 Gated Delta / Global Attention、Dense/MoE交換可能性 |
| OpenAI | 外部router型の計算予算制御 |
| Anthropic | 同一主体内adaptive thinking |

## 4. 暫定実行経路

```text
入力
↓
言語アドレス化
↓
Dense constitutional core
↓
状態更新（causal ledger + gated delta）
↓
文脈選択（content retrieval + sparse top-k）
↓
深さ再利用（stage/depth memory）
↓
Sparse specialist routing
↓
Global reconcile
↓
HDS authority gate
↓
条件付き結果面
↓
出力
```

## 5. 失敗条件

- Dense coreが固定canned responseへ退化する
- specialistが証拠なしで最終出力権限を持つ
- unknownを補完してPASSする
- routerが必要な能力を選べない
- state更新が文脈に反応しない
- global reconcileなしで局所候補を確定する

## 6. 次の実装Gate

1. 日本語parserを強化
2. 実検索connectorを接続
3. operator inventoryのhot-swap
4. Dense core＋Sparse specialist統合MVP
5. 長期memoryと権限管理
6. real benchmarkを構造別に分類して評価


---

# 検訴結果と主張境界

## 1. 検証結果

| 対象 | 結果 |
|---|---|
| Layer-0 v4提案実装 | 20試験合格、strict manifest negative control合格 |
| K3完全版 | 20/20、責任5/5、negative control 8/8 |
| Llama 3完全版 | 23/23、self-test 10/10、責任5/5、negative control 8/8 |
| DeepSeek V3.2参照版 | self-test 7/7 |
| Qwen3.6参照版 | MoE/Dense合計10/10 |
| 5モデル同一toy課題 | 5/5で状態・答え一致 |

## 2. 直接成立した主張

- NN、Transformer tensor、embedding、GPUなしで閉じた言語処理経路を構成できる
- 異なる公開architectureを機能責任へ分解し、非ニューラル機構へ再投影できる
- unknownとcontradictionを`SUSPEND`へ送れる
- output surface遮断を`FAIL`へ送れる
- 同じ局所機能を異なるcompute topologyで成立させられる

## 3. 未成立

- 実モデルweightの意味復元
- frontier scaleの知識・性能・速度
- open-domain benchmark
- multimodal semantics
- 市場用語としてのLLM最終認定
- Dense構造と主体性の因果証明

## 4. 証拠・推論・仮説の分�6

### 証拠

- 公開config、model card、reference code、paperで観測した構造
- ローカル参照実装のtest結果

### 推論

- K3＝多軸選択的情報流
- Llama 3＝Dense連続構成
- Qwenがhybrid attentionとMoEの独立性を示す

### 仮説

- Dense shared bodyが主体的一貫性へ寄与する
- Dense core＋Sparse specialist＋HDS gateが統合本命である

## 5. 確信度

```text
非ニューラル閉路の局所成立               99%
K3多軸選択構造の抽出                     95%
K3固有性としてのAttnRes                  97%
MoEがLLM必要条件ではない                 99%
Dense shared bodyと主体的一貫性の関係     74%
統合アーキテクチャ案                     93%
```

確信度は校正済み確率ではなく、証拠強度・整合性・検証状況の監査目安である。
