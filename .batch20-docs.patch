--- a/評価/命題会話_第20バッチ_実装検証_2026-09-12.md
+++ b/評価/命題会話_第20バッチ_実装検証_2026-09-12.md
@@ -8,7 +8,7 @@
 
 接続済みGitHubのmainを読み取りで確認し、添付の全追跡ソースと第17〜19バッチ差分・確定済み文書から復元した。復元した全Git treeが上記main内容と一致した。実パッケージと実公開HDSを利用する。疑似Compilerや疑似実行器で動作確認を代用しない。
 
-作業はサンドボックス限定。GitHub書込み・PR・CI・main還元は行わない。変更前の全回帰は独立した基点作業ツリーで実施した。変更後の確定結果は末尾に追記する。
+作業はLinux / Python 3.13.5のサンドボックスで実装・検証し、2026-09-12にPR #94を経由してmainへ還元した。変更前の全回帰は独立した基点作業ツリーで実施した。変更後の確定結果とGitHub受入結果は末尾に分離して記録する。
 
 ## 実装
 
@@ -38,7 +38,7 @@
 
 自然文は対応構文に限定。未知修飾、資料自身の曖昧さ、交互量化、一般の等号推論・選言除去・対偶・任意因果・自由作文は未完成。未導出は実装された導出方式で未確定という意味で、絶対的な導出不可能性の証明ではない。
 
-報告の合格と問いの支持を区別する。資料の真実性・出典真正性やGPT-4同等性は認定しない。外部取得は今回使わず、Windows・他Python版・実Chromium・公開Web・GitHub CIは未実施。
+報告の合格と問いの支持を区別する。資料の真実性・出典真正性やGPT-4同等性は認定しない。外部取得は今回使わず、公開Web LIVE品質とGPT-4比較は未実施。Windows・他Python版・実ChromiumはPR #94のCIで受入確認した。
 
 ## 確定したサンドボックス結果
 
@@ -72,4 +72,15 @@
 
 本節の検証書追記は全回帰完了後の文書変更であり、実装・試験ソースは変更していない。配布時には追記した文書も含む完成treeをmanifestへ固定し、最終差分をさらにクリーンな基点へ適用してtreeと実装ソースを再照合する。ハッシュは内容同一性を検査するもので、外部署名や真正性の証明ではない。
 
-再適用ログ: `apply-check.log`、`apply-replay.log`、`apply-negative.json`、`reapply-full.log`と同名の終了記録。最終配布同一性は`package-final-check.json`へ分離する。GitHubへの送信や還元は行っていない。
+再適用ログ: `apply-check.log`、`apply-replay.log`、`apply-negative.json`、`reapply-full.log`と同名の終了記録。最終配布同一性は`package-final-check.json`へ分離する。
+
+## GitHub受入・main還元
+
+2026-09-12、検証済み差分を一時輸送workflow run `34647208810` で再構成した。圧縮差分SHA256・解凍差分SHA256・基点commit・完成treeを再検査し、最終作業branchのtreeが配布manifest固定値 `ce419bb70ee34b3724b258e7d1f97605c3e34886` と一致した。一時workflow・輸送ファイルはPR #94の差分へ含めていない。
+
+- PR #94 `feat: 命題候補・導出説明を統合（第20バッチ）`。
+- 再構築CI run `34647294734`: Ubuntu / Windows × Python 3.11–3.14 の全8構成でsuccess。各構成でリポジトリ整合性、日本語基底、構文、全単体試験、legacy規模測定、module / console CLI smokeを通過した。
+- ブラウザCI run `34647294744`: Ubuntu / Windows の両構成でsuccess。実Chromiumの描画・ページ移動・遮断・HDS接続とローカル描画デモを通過した。
+- main merge commit: `98613a235b37d65c7802f7510d0aa21915112de8`。merge treeは `ce419bb70ee34b3724b258e7d1f97605c3e34886` で、サンドボックスで検証した完成物と一致する。
+
+還元時にこの対話環境で独立再実行した全回帰は外側待機上限で完走前に打ち切られたためPASSとは記録しない。サンドボックス作成時の完走記録は2,107件中2,083件成功・24件SKIPであり、還元時の全構成受入は上記GitHub CIで確認した。公開Web LIVE取得とGPT-4比較は引き続き未実施であり、本還元の成立証拠へ読み替えない。
--- a/docs/汎用Capability_開発進捗.md
+++ b/docs/汎用Capability_開発進捗.md
@@ -283,4 +283,4 @@
 検証: [第20バッチ](../評価/命題会話_第20バッチ_実装検証_2026-09-12.md)
 デモ: `python tools/命題会話デモ.py`
 
-今回の作業はサンドボックス。GitHub書込み・PR・CI・main還元は行わない。
+状態はmain還元済み。実装はPR #94、再構築CI run 34647294734、ブラウザCI run 34647294744 を経てmainへ統合した。実装merge commitは `98613a235b37d65c7802f7510d0aa21915112de8`、実装treeは `ce419bb70ee34b3724b258e7d1f97605c3e34886`。公開Web LIVE取得とGPT-4比較は未実施。
--- a/docs/汎用チャット_実装ロードマップ.md
+++ b/docs/汎用チャット_実装ロードマップ.md
@@ -81,4 +81,4 @@
 
 次に残るのは、資料本文側の候補保持・文脈による解消、引用・話者・伝聞・語義を含む命題抽出、外部取得した文章と命題エンジンの接続、一般的な論証・評価・作文の内容構成、数式/コード/翻訳の能力拡張である。これらを名称やテンプレートだけで成立扱いしない。
 
-詳細は設計56と第20バッチ検証記録へ分離する。今回GitHubへの送信・CI・main還元は行っていない。
+詳細は設計56と第20バッチ検証記録へ分離する。第20バッチはPR #94、再構築CI run 34647294734、ブラウザCI run 34647294744 を経てmainへ還元済み。実装merge commitは `98613a235b37d65c7802f7510d0aa21915112de8`、実装treeは `ce419bb70ee34b3724b258e7d1f97605c3e34886`。公開Web LIVE取得とGPT-4比較は未実施。
