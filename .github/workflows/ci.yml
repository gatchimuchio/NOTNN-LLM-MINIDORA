name: MINIDORA 再構築CI

on:
  push:
    branches: [main]
    paths-ignore:
      - ".benchmark/GPQA_REQUEST.txt"
  pull_request:
  workflow_dispatch:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ['3.11', '3.12', '3.13', '3.14']
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: パッケージ導入
        run: python -m pip install -e .
      - name: リポジトリ整合性監査
        run: python tools/repository_consistency_check.py
      - name: 日本語基底監査
        run: python tools/日本語基底監査.py
      - name: 構文確認
        run: python -m compileall -q src tests tools
      - name: 単体試験
        run: python -m unittest discover -s tests -v
      - name: v0.4規模測定 legacy regression
        run: python tools/規模測定.py
      - name: module CLI smoke test
        run: python -m minidora "2+3"
      - name: console script smoke test
        run: minidora "2+3"
