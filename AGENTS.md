# AGENTS.md — MINIDORA 実装・監査規約

## 1. Authority

矛盾時の優先順位:

1. ユーザーの明示指示
2. 最上位理論正本 `cognitive-engineering-foundations`
3. 責任別外部正本 `LLM-Constitutive-Specification`
4. `設計/` の現行MINIDORA局所契約
5. `src/minidora/` の現行実装
6. `tests/` / `評価/` の実測・回帰結果
7. `構文化/` の観測・再構成履歴
8. `artifacts/` の固定取得物・由来記録
9. 推測・一般論

### 最上位理論正本

- Repository: https://github.com/gatchimuchio/cognitive-engineering-foundations
- 参照commit: `60131da52ba7931ed7f82c7648a74ac790f50d08`
- 基底言語・規定言語: 日本語

```text
日本語で理論生成・対象化・差異化・関係化
→ 日本語で設計・構文化・監査・改訂
→ 日本語正本成立
→ 実務上やむを得ない外部境界だけ多言語を例外使用
```

外国語を第二基底・並列正本・内部意味の主語として扱わない。

### 言語模型成立条件の責任正本

- Repository: https://github.com/gatchimuchio/LLM-Constitutive-Specification
- 版: `2026-08-28-成立規定-8`
- 参照commit: `fcbc2fa4bc89d749942e8ebee2764115488d29c4`
- 正本: `規定/02_大規模言語模型成立.md`
- 能力作用構成: `規定/07_能力作用構成.md`

MINIDORA内の参照定数は `src/minidora/規定参照.py` を正とする。

Authorityは責任衝突を解く順序であり、理論・構文化・Compiler・MINIDORA・実測を固定直列化しない。重大な新観測があれば必要な前提まで再開放する。

## 2. 日本語基底

`設計/00_日本語基底規定_v1.md` を局所規定とする。

基本言語、規定言語、意味正本、設計言語、構文化言語、監査言語、独自内部概念名は日本語とする。

多言語は、予約語、外部API固定名、規格、外部模型・論文の固有名、URL、hash、言語コード、外部検索、国際公開等の実務上必要な境界だけで認める。

`ja / en / zh` は外部互換識別コードであり、内部意味正本ではない。

## 3. 現行二核

```text
MINIDORA厳密言語模型
= 完全言語状態上の整合した確率法則

MINIDORA能力模型核
= 候補・証拠・関係評価 / 推論 / 候補選択
```

候補得点・HDS寄与・GPQA得点を確率へ変換し、厳密言語模型成立証拠へ読み替えない。

## 4. 厳密言語模型受入境界

- 完全言語状態空間を宣言する。
- 持続模型状態を持つ。
- 整合した言語確率法則を定義する。
- 局所条件しかない場合、完全法則への接続を確認する。
- 可変長では終端境界を閉じる。
- 模型状態保存・復元で同じ法則を再現する。
- 非ニューラル・決定論性を確率法則と矛盾扱いしない。

## 5. 能力作用構成

構成定義v8の観測単位:

```text
状態担体
作用
状態差
後続利用
参照変更
経路変更
計算量変更
再参照
再結合
循環尺度
```

次を混同しない。

```text
状態の存在
≠ 後続利用
≠ 後続作用の実発火
≠ 能力改善
```

## 6. HDS Compiler境界

現行公開Compiler:

```text
Architecture v1.3
Pipeline v1.4
規定言語 = 日本語
基底言語 = 日本語
基底言語コード = ja
```

Compilerは意味IR・監査構造・作用差分構造を生成するが、後続作用を実行せず、候補採否・最終判断もしない。

作用差分構造:

```text
作用
→ 状態差
→ 後続利用
```

後続利用は「後状態が次作用の入力状態条件を満たす」構造的接続であり、追加条件充足や発火を確定しない。

Pipeline v1.4は `意味IR / 計算計画 / 作用差分構造` を別フィールドで保持し、作用差分を計算Pへ自動降下しない。

## 7. 計算境界

旧Layer0は計算実行器互換名。

```text
日本語命令形P
→ 計算中間表現
→ 計算実行境界
→ 計算実行器
```

## 8. 構文化

現行K3除外横断観測は `構文化/言語模型横断_日本語基底作用構文化_v3/`。

外部技術語は固有名・出典照合欄へ限定し、内部観測座標は日本語で保持する。

K3はこのv3再構文化対象外。

## 9. Legacy

過去の構文化、v0.3/v0.4評価、旧Layer-0、旧HDS Pipeline、GPQA実測は削除しない。履歴として残し、現行意味へ無言復帰させない。

## 10. 実装原則

- 日本語で成立する独自内部概念は日本語で記述する。
- 多言語例外は外部互換境界へ局所化する。
- 候補ID・正解ラベル・benchmark固有名を厳密言語模型法則へ埋め込まない。
- 世界知識を最小言語模型の初期資産へ埋め込まない。
- 根拠差がない能力経路で勝手な候補確定をしない。
- PへDataを埋め込まない。
- HDS型を厳密言語模型核へ逆流させない。
- 発火数・非ゼロ計数を目的化しない。
- 実装都合だけを理由に理論正本を曲げない。
- 実測が現行規定を崩す場合は規定自身を再開放する。

## 11. 検証

`main` を現行正本とする。完成差分だけmainへ還元する。

```bash
python tools/repository_consistency_check.py
python tools/日本語基底監査.py
python -m compileall -q src tests tools
python -m unittest discover -s tests -v
python -m minidora "2+3"
```

CIはUbuntu / Windows × Python 3.11–3.14を確認する。
