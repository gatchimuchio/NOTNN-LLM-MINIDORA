# AGENTS.md — MINIDORA 開発・監査規約

## 0. この文書の役割

本書は、MINIDORAを現在の `main` から開発・修正・監査する作業主体が最初に従う規約である。

本書は理論正本、設計履歴、性能報告、版ごとの作業記録を兼ねない。
過去の経緯はGit履歴、`docs/履歴/`、各評価成果物へ残し、本書へ時系列で積み増さない。

作業開始時は必ず現在の `main`、直近の関連コミット、現行実装、試験、責任別正本を確認する。
古い設計文書や過去の性能系列を、現在の全体構成へ無言で復帰させない。

## 1. 正本と根拠の優先

矛盾または意味差がある場合は、次を基準に確認する。

1. ユーザーの明示指示
2. 非公開内部参照
3. `PUBLICATION_BOUNDARY.md` と `設計/00_日本語基底規定_v1.md`
4. 責任ごとの現行正本
5. 現在の `main` の実装・試験・実測
6. 現行設計文書・ロードマップ・台帳
7. Git履歴・旧文書・旧評価成果
8. 推測・一般論

非公開内部参照の参照先、固定版、完全な抽象定義、実装との完全対応表は公開リポジトリへ記載しない。

責任ごとの現行正本は一つに固定しない。少なくとも次を分ける。

- 言語模型成立条件: `src/minidora/規定参照.py` が指す外部正本
- 日本語基底: `設計/00_日本語基底規定_v1.md`
- 日本語語彙: `設計/01_日本語正本語彙_v1.md`
- 共通コア責任: `src/minidora/コア/責任境界.py`
- 標準能力入口の責任境界: `src/minidora/コア/能力境界.py`
- 統合実行入口: `src/minidora/HDS駆動コア.py`
- 実行主体と状態差循環: `src/minidora/HDS実行主体.py`
- 統合実行系の公開境界: `HDS_MINIDORA_現行正本.md`
- 能力開発残差: `docs/HDS全能力開発_ロードマップ.md` と `docs/HDS全能力開発_台帳.json`
- 性能値・評価条件: `評価/` の該当正本

現行実装と文書が食い違う場合、どちらかを推測で正しいことにしない。
履歴と試験を確認し、意図した責任と実装事実を分けて再監査する。

## 2. 日本語基底

MINIDORAが自ら定義する意味は日本語を正本とする。

日本語を正本とする対象には、少なくとも次を含む。

- 仕様・設計・規定
- 内部意味概念
- 状態・作用・関係・境界・採否
- 自前のファイル名、型名、関数名、変数名、属性名
- 内部JSON等の鍵と区分値
- 試験の意味名
- ログの意味区分
- 文書名・成果物名

英語その他の外国語は、外部規格、外部API固定値、外部製品・論文・データセットの固有名、URL、ハッシュ、言語コード、プログラミング言語予約語など、外部互換上必要な境界だけで使う。

旧英字名を残す必要がある場合は薄い互換入口へ閉じ、日本語正本へ委譲する。
旧英字概念を新しい内部意味名として再利用しない。

`AGENTS.md` というファイル名自体は外部作業系との互換境界として保持するが、本文の意味正本は日本語とする。

## 3. 現行の責任構造

現在の能力実行系は、少なくとも次の責任を分けて扱う。

### 3.1 HDS実行主体

HDS実行主体が次を所有する。

- 目的
- 要求状態
- 成立状態
- 残差
- 成果
- 主体状態
- 依存
- 観測要求
- 記憶
- 仮説
- 検証
- 再評価
- 作用の選択・実行
- 停止
- 最終採否

部品が返した局所的な成功、候補、得点、回答ラベルを、そのまま全体の採用へ昇格させない。

### 3.2 HDSコンパイラ

HDSコンパイラは入力を実行系が扱える意味表現へ射影する。

現行の入力正本は `HDSコア入力束` であり、対応する構文化器が利用できる場合はHDS駆動コアの起動前に形成する。

構文化器は次を代行しない。

- 全体目的の最終達成判定
- 能力の最終採用
- 最終回答の採否
- 専門作用そのものの実行
- 未観測事項の推測補完

旧構文化経路は互換用途として残り得るが、現行入力正本より優先しない。

### 3.3 ミニドラ共通コア

共通コアの現行責任は `src/minidora/コア/責任境界.py` の八責任を正とする。

1. 意味・由来・条件交換
2. 作用契約・起動
3. 参照・依存・状態差
4. 関係・束縛・仮説
5. 計画・残差帰還
6. 検証・採否・失敗
7. 実行内適応・反証
8. 内容計画・表現制約

共通コアへ置くのは、複数能力に横断して必要で、意味・状態・作用・採否の共通責任として成立するものに限る。

次は原則として共通コアへ入れない。

- 個別言語や形式の構文解析
- 専門知識
- 専門法則
- 領域固有の探索法
- 個別ソルバーの内部算法
- 外部取得器そのもの
- 語彙選択・文法・文体・翻訳対
- 永続的な一般学習

新しい共通責任を追加する前に、既存八責任へ還元できないかを監査する。
能力追加の都合だけで共通コアを肥大化させない。

### 3.4 能力モジュール・局所部品

能力モジュールと局所部品は、専門知識、専門算法、形式別解析、表現実現、外部取得、局所検証等を担当する。

標準能力入口の責任分類は `src/minidora/コア/能力境界.py` を正とする。
登録数を能力の完成度や知能水準へ読み替えない。

未知の標準入口を未分類のまま実行系へ混入させない。
標準能力と拡張能力を無言で同一視しない。
拡張能力が標準入口を上書きしない。

モジュール内部に全体の状態所有、全体計画、最終採否を重複実装しない。
共通責任をコアへ移す場合も、専門責任までコアへ吸収しない。

## 4. 現行循環

現行の基本関係は次である。

```text
入力
↓
HDSコンパイラによる入力正本形成
↓
HDS実行主体が目的・状態・残差を所有
↓
現在状態から実行可能な作用を供給
↓
参照・計算・模型・能力モジュール等を局所作用として実行
↓
成果・状態差・失敗・残差をHDSへ帰還
↓
検証・再観測・再計算・再評価
↓
閉包条件を満たした場合だけ最終採用
```

未閉包は失敗と同義ではない。
不足が観測・計算・追加作用で解消可能なら、残差として保持して次作用へ接続する。

同じ入力に対して既に成立した結果を、追加処理だけを理由に無証明で破壊しない。
非退行基準は可変成果と混同せず、現行実装の主体状態・非退行契約に従う。

## 5. 旧系統の扱い

次は現行全体構成の定義ではない。

- MINIDORA30
- MINIDORA80
- 旧HDS監督介入経路
- 版ごとのv1〜v6増分文書
- 過去の能力登録数
- 過去のGPQA値
- 旧構文化系列

MINIDORA30とMINIDORA80は性能評価系列として保持する。
統合実行系とは別系列であり、相互の性能値を転記しない。

旧HDS監督介入経路は互換性能再現の履歴である。
「通常MINIDORAを先に実行し、未閉包時だけHDSが後から監督する」構造を、現行統合実行系へ復活させない。

v1〜v6の運用文書は各時点の増分契約と履歴である。
現在の全体アーキテクチャは、現行ソース、責任境界、試験、直近の統合履歴を確認して判断する。

## 6. 学習・経験・推論・適応

非公開内部理論上の完全な学習定義・推論定義は本書で再定義しない。

公開実装では少なくとも次を混同しない。

```text
実行記録
!= 経験
!= 記憶
!= 推論
!= 実行内適応
!= 永続学習
```

現行コア責任の「実行内適応・反証」は、同一定義・同一意味入力の実測効果を現在実行内で利用し、失敗や無効化条件で期待を撤回する責任である。
これを永続学習、一般化済み知識、条件を越えた経験転用へ読み替えない。

自動形成・再利用は、明示的に純粋性と入力条件が契約された作用だけを対象にする。
外部取得、可変外部状態、権限付き作用を無条件に純粋再利用しない。

学習機構を追加する場合は、少なくとも入力条件、保持状態、状態差、採用条件、失効条件、反証時の扱いを明示し、単なる保存や回数増加を学習成立としない。

## 7. 能力開発

能力開発では、まず既存実装を全数監査し、次を分ける。

- 既に成立している能力
- 共通コアへ移すべき責任
- モジュールとして残す専門責任
- HDSコンパイラが担う入力変換責任
- 接続だけが不足しているもの
- 実装そのものが不足しているもの
- 試験だけ不足しているもの
- 未観測

新しいモジュールを足す前に、既存部品の重複、責任漏れ、接続漏れを確認する。

能力名、空の接続口、試験用の固定分岐、停止機構だけを能力完成として数えない。
意味表現を追加する場合は、その表現を実際に消費する後続作用まで接続する。

現在の能力開発残差と順序は `docs/HDS全能力開発_ロードマップ.md` と `docs/HDS全能力開発_台帳.json` を使う。
ただし、これらは能力進捗の台帳であり、統合実行系の全体アーキテクチャ正本ではない。

## 8. 観測・判断・主張

次を分けて記録する。

- 観測事実
- 実測結果
- 根拠
- 推論
- 仮定
- 未確認
- 未実装
- 失敗

局所試験の成功を全体性能へ拡張しない。
能力登録数、作用発火数、試験件数、非ゼロ得点を性能指標そのものにしない。

検証器が保証していない正しさを推定で付与しない。
取得成功を資料内容の真実性へ読み替えない。
資料内支持を世界事実へ読み替えない。
世界事実の確認と、採用する意思決定を混同しない。

重大な新観測が現在の責任分解を崩す場合は、実装へ例外を足して逃げず、責任境界・前提・設計を再開放する。

## 9. 評価と汚染防止

評価課題の正解、正解ラベル、問題識別子、固定正解表を実行時判断へ渡さない。

GPQA等の性能値は `評価/` の現行正本成果物からのみ引用する。
固定参照資料を禁止している現行評価系列へ、保存済み検索結果や過去参照を再投入しない。

部分実行、診断実行、再生試験の値を正本性能へ読み替えない。
評価系列を更新する場合は、系列名、条件、入力、参照方式、成果物を保存し、別系列を無言置換しない。

## 10. 公開境界

公開文書と公開コードから、非公開内部理論の完全定義や完全対応表を復元できる形にしない。

公開側へ書くのは原則として次までとする。

- 入力
- 出力
- 状態差
- 依存
- 資源
- 権限
- 失敗条件
- 保証すること
- 保証しないこと
- 再現に必要な公開情報

内部理論上の完全な最小分解、上位参照先、固定版、非公開概念との一対一対応を記載しない。

公開前に `python tools/公開境界監査.py` を実行する。

## 11. 作業手順

実装作業は次の順で行う。

1. 現在の `main` のコミットと木を固定する。
2. `AGENTS.md`、日本語基底規定、公開境界、対象責任の正本を読む。
3. 対象ファイルに関係する直近コミットと旧実装を確認する。
4. 現行コードと試験から、成立済み・不足・未観測を分ける。
5. コア、コンパイラ、モジュール、外部境界のどこに責任を置くか先に決める。
6. 最小差分で実装し、旧成果・旧正本・旧報告を無言で書き換えない。
7. 正常、反例、未知、失敗、失効、再評価、保存復元、権限、予算の必要範囲を試験する。
8. 日本語基底、公開境界、リポジトリ整合性、全回帰を確認する。
9. 能力進捗が変わった場合はロードマップ・台帳・検証記録を更新する。
10. 完成した差分だけ `main` へ還元する。

並行更新があれば最新 `main` を取得し直し、差分と試験を再確認する。
他者差分を上書きする強制更新をしない。

## 12. 基本検証

最低限、次を確認する。

```bash
python tools/リポジトリ整合性監査.py
python tools/日本語基底監査.py
python tools/日本語基底詳細監査.py
python tools/公開境界監査.py
python -m compileall -q src tests tools
python -m unittest discover -s tests -q
python -m minidora "2+3"
```

共通コア責任または標準能力境界を変更した場合は、少なくとも次も確認する。

```bash
python -m unittest discover -s tests -p 'test_MINIDORAコア閉包_v3.py' -v
```

HDS入力・駆動・正本継承を変更した場合は、少なくとも次も確認する。

```bash
python -m unittest discover -s tests -p 'test_HDSコア入力優先_v1.py' -v
python -m unittest discover -s tests -p 'test_HDS正本継承循環_v1.py' -v
python -m unittest discover -s tests -p 'test_HDS実行主体_v1.py' -v
```

継続的統合のUbuntu / Windows試験と、利用者Windows実機の確認は別物として扱う。
人工参照・人工HTTP試験と、公開Web・実サービス上の実運用も別物として扱う。

## 13. 完了条件

変更を完了とするのは、必要な観測と検証を行い、次を確認した場合だけとする。

- 対象責任が明確
- 既存責任との重複・漏れがない
- 日本語基底を満たす
- 公開境界を破らない
- 局所成功を全体成功へ誤読していない
- 既存成立機能を無証明で退行させていない
- 未確認事項が主要判断を変えない
- 試験・評価の射程を越えた主張をしていない

条件を満たさない場合は、未完了・未確認・保留として残す。
