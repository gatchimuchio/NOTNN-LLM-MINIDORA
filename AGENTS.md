# AGENTS.md

## リポジトリ方針

- 正本ブランチは `main` のみとする。
- 作業ブランチを常設しない。
- 必要な変更は最終的に `main` へ集約し、不要になった作業ブランチは削除する。
- 既存仕様・構文化・評価結果・正本パッケージを上書きまたは削除するときは、先に実物と履歴を確認する。
- 整理目的だけで構文化正本、固定成果物、評価記録を削除しない。

## 正本階層

MINIDORA内の文書・実装を同格に扱わない。

1. ユーザーの明示指示
2. 外部Layer-0正本の参照version / commit
3. `設計/` のMINIDORA局所契約
4. `src/minidora/` の実装
5. `tests/` と `評価/` の検証記録
6. `構文化/` の観測・再構成成果
7. `artifacts/` の固定取得物・派生成果

Layer-0以外の領域へこの優先順位を機械的に一般化しない。詳細は `REFERENCES.md` と `設計/README.md` を参照する。

## Layer-0参照規則

- MINIDORAにおけるLLM機能責任の論理上位契約は `gatchimuchio/LLM-Layer-0-Functional-Compliance-Specification` とする。
- 現行仕様は `v4.0-provisional`、現行参照commitは `4adf86d13d7beb99fe5eaa9e240b22996ba3d3bc` とする。
- `設計/02_Layer0責任契約.md` はMINIDORAへの局所写像であり、Layer-0そのものを再定義しない。
- Layer-0責任の意味を変更するときは、先に外部正本を確認する。
- 外部Layer-0の更新へ自動追従しない。意味差分・実装・試験を監査した後に参照commitを更新する。
- `構文化/MINIDORA_v0.3/Layer0/` をLayer-0上位正本として扱わない。

## 言語

- README、設計、構文化、評価、運用文書は日本語を基底言語とする。
- API、規格、コード識別子、固有名詞、原文確認が必要な箇所は正確性を優先して原語を保持する。
- 既存の日本語識別子を理由なく英語へ置換しない。
- 英語ファイル名・識別子が外部互換や既存契約として機能している場合は、整理目的だけで改名しない。

## 構造境界

- `設計/` は現行MINIDORAの契約、`構文化/` は観測・再構成成果、`評価/` は実測記録として分離する。
- `docs/` は補助的な実装・接続ノートとし、`設計/` の代替正本にしない。
- `artifacts/` は固定取得物・派生成果を保持し、Runtimeソースと混同しない。
- 構文化資料と MINIDORA 実装物を混同しない。
- HDS 構文化資料を Layer-0 / P / Adapter へ自動変換しない。明示的な指示がある場合のみ後段処理へ進む。
- 公開構文化成果と、公開対象外の上流解析方法を混同しない。
- Legacy成果は削除せず、現行正本と明確に区別する。

## 変更時の整合性

- Runtime version、Layer-0 version、参照commit、README、設計文書、試験の食い違いを残さない。
- プロトタイプ完成記録は履歴固定値であり、将来の性能値で上書きしない。
- `PROTOTYPE COMPLETE` と製品・最終完成を混同しない。
- 文書リンク、正本参照、主要ディレクトリ境界は `tools/repository_consistency_check.py` で検証できる状態を維持する。

## ライセンス・著作

- MINIDORA の独自実装および本リポジトリで作成した独自文書のライセンスは Apache License 2.0 とする。
- 著作名は `がっちむち♂` とする。
- 第三者由来資料・公開データ・モデル関連成果物は、各出典のライセンスおよび利用条件を上書きしない。
- 第三者由来物を追加するときは、可能な範囲で source lock / manifest / provenance を保持する。
