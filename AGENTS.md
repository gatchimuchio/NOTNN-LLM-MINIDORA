# AGENTS.md — MINIDORA 作業標準仕様

## 1. 基本

本書はMINIDORAの開発・修正・監査・評価における作業標準仕様である。

- ユーザーの明示指示を最優先する。
- 日本語を内部意味の正本とする。外国語は外部固定名・規格・識別子等に限定する。
- 対象・目的・手段を分け、手段を目的化しない。
- 事実・実測・推論・仮定・未確認を混同しない。
- 局所成立を全体成立へ読み替えない。
- 現行仕様・実装を完成形として固定せず、新しい観測に応じて再評価する。

## 2. 作業環境

GitHubは正本・履歴・版管理の場所であり、作業場所として使用しない。

実装・修正・通常検証は必ずサンドボックスで行う。

サンドボックスからGitHubへの直接通信は行わない。`git clone / pull / push` は使用せず、GitHub API経由で対象版を取得・反映する。

標準経路は次とする。

```text
GitHub APIでmainの対象版を取得
→ サンドボックスへ再構成
→ サンドボックスで調査・実装・試験
→ 差分確認
→ GitHub APIでmainへ直接反映
```

通常は `main` 一本で運用し、作業用ブランチ・PRを作成しない。

## 3. 作業開始

変更前に必要な範囲で確認する。

1. ユーザー指示
2. 現在の `main`
3. 関連仕様・正本
4. 関連コード・試験
5. 関連履歴・実測

取得可能な根拠を確認せず、記憶・一般論・推測だけで実装しない。

既存機能を確認し、能力不足・接続不足・責任配置・状態伝達・検証不足を分ける。
既存機能と同じものを重複実装しない。

## 4. 実装

```text
現状観測
→ 問題・不足の特定
→ 変更責任の決定
→ 設計
→ 実装
→ 必要な検証
→ 差分確認
→ mainへ反映
```

下流修正で閉じない問題は、必要な上流まで戻って再設計する。

次を禁止する。

- 未確認事項の推測補完
- 評価用正解・問題固有情報の実装混入
- 固定入力専用処理の一般能力化
- 局所成功の最終採用への直結
- 失敗の握り潰し
- 旧意味・旧保存物の無言変換
- 既存実装を確認しない代替実装の新造
- 要求外の変更・整理・再設計

## 5. 検証

通常の品質担保は作業者自身がサンドボックスで完了させる。

変更内容に必要な試験・回帰・監査だけを実行する。
試験を目的化せず、変更と無関係な再検査を行わない。

GitHub Actionsの使用は次に限定する。

- **受け入れ検査**
  - Windows / Linux / macOS 等の環境差
  - GitHub・配布・実環境固有の成立確認
- **重い処理**
  - ベンチマーク
  - 全数評価
  - 長時間・高負荷処理

通常の単体試験・回帰・静的検査・文書確認をGitHub Actionsへ代行させない。
同じ試験を場所を変えて繰り返すだけの検査や、検査結果を再検査する再帰的処理を行わない。

## 6. 履歴・公開

開発正本の節目は `開発正本履歴.md` とGit commitで管理する。
過去の作業ログ、移行記録、一時監査、旧評価個票をdefault treeへ重複保存しない。
過去コードが必要な場合は正本履歴に固定したcommitから復元する。

`構文化/` はMINIDORAの構築原料として保全し、一般の開発ログと同じ扱いで削除しない。

非公開内部理論の完全定義・参照先・実装対応表を公開物へ記載しない。
公開範囲は `PUBLICATION_BOUNDARY.md` に従う。

## 7. 完了

次を満たした場合だけ完了とする。

- ユーザー要求が成立している。
- 必要な実装が完了している。
- サンドボックスで必要な品質確認が完了している。
- 必要な場合だけ受け入れ検査・重い処理を実施している。
- 主要な未確認事項が残っていない。
- 既存成立機能を根拠なく退行させていない。
- 日本語基底と公開境界を守っている。
