# AGENTS.md

## Repository policy

- 正本ブランチは `main` のみとする。
- 作業ブランチを常設しない。
- 変更が必要な場合も、完了後は成果物を `main` へ集約し、作業ブランチを削除する。
- 既存仕様・構文化・評価結果を上書きするときは、先に実物を確認する。
- 構文化資料とMINIDORA実装物を混同しない。
- HDS構文化資料をLayer-0 / P / Adapterへ自動変換しない。明示的な指示がある場合のみ後段処理へ進む。
