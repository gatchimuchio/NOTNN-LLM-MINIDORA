# AGENTS.md — MINIDORA 実装・監査規約

## 1. Authority

矛盾時の優先順位は次とする。

1. ユーザーの明示指示
2. 最上位理論正本 `cognitive-engineering-foundations`
3. 責任別外部正本 **大規模言語模型成立規定**
4. `設計/` の現行MINIDORA局所契約
5. `src/minidora/` の現行実装
6. `tests/` / `評価/` の実測・回帰結果
7. `構文化/` の観測・再構成履歴
8. `artifacts/` の固定取得物・由来記録
9. 推測・一般論

### 最上位理論正本

- Repository: https://github.com/gatchimuchio/cognitive-engineering-foundations
- 参照commit: `60131da52ba7931ed7f82c7648a74ac790f50d08`
- 基底言語・規定言語: 日本語

原則:

```text
日本語で理論生成・対象化・差異化・関係化
→ 日本語で設計・構文化・監査・改訂
→ 日本語正本を成立
→ 実務上やむを得ない外部境界だけ多言語を例外使用
```

外国語を第二基底・並列正本・内部意味の主語として扱わない。

### 言語模型成立条件の責任正本

- Repository: https://github.com/gatchimuchio/LLM-Constitutive-Specification
- 版: `2026-08-28-成立規定-7`
- 参照commit: `debb83e091a705a5eac09ef4fb97a5b36305db6d`
- 正本: `規定/02_大規模言語模型成立.md`

MINIDORA内の現行参照定数は `src/minidora/規定参照.py` を正とする。v0.4以前のファイルに残る旧版記録を現行Authorityへ昇格しない。

Authorityは責任衝突を解く順序であり、理論・構文化・Compiler・MINIDORA・実測を一方向の直列系として固定するものではない。重大な新観測・実装結果が得られた場合は、必要な正本・前提まで再開放して再監査する。

## 2. 日本語基底

`設計/00_日本語基底規定_v1.md` をMINIDORA局所規定とする。

基本言語、規定言語、意味正本、設計言語、構文化言語、監査言語、独自内部概念名は日本語とする。

多言語を認めるのは、プログラミング言語予約語、外部API固定名、規格名、外部模型・論文の固有名、URL、hash、言語コード、外部検索・国際公開等、実務上必要な境界だけである。

`ja` / `en` / `zh` 等の言語コードは外部互換識別子であり、内部意味正本ではない。

外部原語を取り込むときは、先に日本語の対象・状態・作用・関係・境界へ射影する。日本語側で意味分別できない場合は推測で固定せず保留する。

## 3. 現行二核

v0.5では次を分離する。

```text
MINIDORA厳密言語模型
= 完全言語状態上の整合した確率法則

MINIDORA能力模型核
= 候補・証拠・関係評価 / 推論 / knowledge choice
```

旧 `MINIDORA模型核` / `模型核` は能力核互換名であり、厳密言語模型核を意味しない。

候補得点、HDS寄与、GPQA得点をsoftmax等で確率へ変換し、厳密言語模型成立証拠へ読み替えない。

## 4. 厳密言語模型受入境界

最低限、次を満たす。

- 完全言語状態の標本空間を宣言する。
- 各接頭辞条件分布が厳密に1へ正規化される。
- 系列確率が連鎖則で一貫して計算できる。
- 可変長では終端確率が正で、無限長へ確率質量が漏れない。
- 模型状態を保存・復元して同じ確率法則を再現できる。
- samplingを模型成立条件にしない。
- 非ニューラルであること、決定論的に確率値を算出することと、確率法則を持つことを矛盾扱いしない。

外部規格上の `EOS`、`SHA-256` 等は必要な互換名として使用できる。

## 5. 能力・Large・呼称を分ける

```text
厳密言語模型成立
!= 推論能力
!= GPQA性能
!= Large
!= 現代LLM呼称適合
```

旧v0.4三面規模測定は履歴証拠として保存するが、v0.5のLarge成立証拠へ無言転用しない。

## 6. HDS境界

HDS Compiler / HDS判断主体 / 参照Rは厳密言語模型法則と分離する。

knowledge choice互換経路では能力模型核を利用する。後段HDSへ渡すのはMINIDORA能力出力だけとし、保留・拒否後の差し戻しを現行経路へ無言追加しない。

HDS Dataや候補寄与を厳密言語模型の形成済み確率へ実行時に自動昇格しない。

## 7. 計算境界

旧Layer0は計算実行器互換名であり、厳密言語模型核ではない。

```text
日本語命令形P
→ 計算中間表現
→ 計算実行境界
→ 計算実行器
```

を維持する。

## 8. 構文化

現行K3除外横断観測は `構文化/言語模型横断_日本語基底作用構文化_v3/` とする。

外部技術語は固有名・出典照合欄に限定し、内部観測座標は日本語で保持する。

```text
状態担体
作用
状態差
後続利用
参照変更
経路変更
計算量変更
再参照
再結合
循環尺度
```

を必要に応じて分別し、「再作用」等の一語へ異種現象を無差別に圧縮しない。

K3は今回のv3再構文化対象外であり、既存K3正本へ無言変更を加えない。

## 9. Legacy

過去の構文化、v0.3/v0.4評価、旧Layer-0、構成再現v3、GPQA実測は削除しない。履歴として残し、現行意味へ無言復帰させない。

`構文化/LLM横断_状態差作用構文化_v2/` は技術観測履歴として保存するが、外部原語が内部座標へ残ったため現行日本語基底構文化には使用しない。

## 10. 実装原則

- 日本語で成立する独自内部概念は日本語で記述する。
- 多言語例外は外部互換境界へ局所化する。
- 候補ID・正解ラベル・benchmark固有名を厳密言語模型法則へ埋め込まない。
- 世界知識を最小言語模型の初期資産へ埋め込まない。
- 根拠差がない能力経路で勝手な一候補確定をしない。
- PへDataを埋め込まない。
- HDS型を厳密言語模型核へ逆流させない。
- 実装都合だけを理由に理論正本・成立規定・日本語基底規定を曲げない。
- 実測が現行規定を崩す重大な観測を示した場合は、規定自身を再開放して監査する。

## 11. 正本ブランチと検証

`main` を唯一の現行正本とする。変更前はサンドボックスで検証し、完成差分だけmainへ還元する。

最低限:

```bash
python tools/repository_consistency_check.py
python tools/日本語基底監査.py
python -m compileall -q src tests tools
python -m unittest discover -s tests -v
python -m minidora "2+3"
```

CIはLinux / Windows × Python 3.11–3.14を確認する。
