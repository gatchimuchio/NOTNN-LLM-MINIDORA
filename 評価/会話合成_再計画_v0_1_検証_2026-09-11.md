# 会話合成・再計画 v0.1 — 第18バッチ実装記録

日付: 2026-09-11
基点commit: `7eb3eeefe07fcbb033d232d27b9e14f553a02b3f`
基点tree: `78b4152f2ed0028004d478991655ab92ba2a6d3d`

## 取得と実装環境

Linux / Python 3.13.5のサンドボックス。直接GitHub接続はDNS解決に失敗したため、接続済みGitHubから既存ソースartifact `10181612716`（取得run `34553375274`）を取得し、第17バッチの検証済み13ファイルを重ねた。各Git blob、実行属性を照合し、復元した全追跡treeが基点mainのtreeと一致した。

実際のパッケージ、公開HDS、既存29部品、既存合成器と採用境界を読み込んだ。部分namespaceや擬似HDSで本体を代替していない。前の未完了作業の設計・差分を引き継ぎ、整合確認・実装修正・再実行を行った。

## 実装した接続

二資料の数値比較、数値照会、単位確認・補充、条件と時点の保持、明示訂正、手元資料の追加後の再開、資料不足から外部取得への対象別再計画、回答意味と文章化、旧数学・文書等の目的経路の接続、製品CLIの明示選択入口を追加。

二資料不足の接続では局所A不足→A取得→局所B不足→同一要求のA取得を再参照してB取得→比較・回答を実行した。対象別に検索が各一度、同じ目的条件hashのまま3試行となった。

## 動作確認

| 対象 | 結果 |
|---|---|
| 変更前全回帰 | 1,742件中1,718件成功・24件SKIP / 53.369秒 |
| 最初の局所確認 | 80件成功（既存7件を含む） |
| 追加後の局所確認 | 110件成功（新規103＋既存7） / 9.344秒 |
| 中間の全回帰 | 1,845件中1,821件成功・24件SKIP / 61.562秒 |
| 回答節の拡張後の局所確認 | 114件成功（新規107＋既存7） / 9.377秒 |
| リポジトリ整合性・日本語基底・compileall | PASS |
| 既存規模測定・既存`python -m minidora "2+3"` | PASS |

局所パターン`test_*18.py`には従来の`test_relation_qualifiers_v18.py`7件も含まれる。新規数を114とせず107件に訂正して区別した。既存回帰の削除はない。24件は任意ブラウザ試験のSKIPであり成功件数へ算入しない。内部全列挙の反復を別の試験数に加算しない。

最終全回帰の結果は下記の追記に固定する。未完走を成功に読み替えない。

## 試験の中身と制限

- 実公開HDSを通した要求・質問・単位補充・条件訂正、未知尾部や否定の非切捨て。
- 人工作用の独立した直積列挙と計画費用の一致、左右役割・中間共有・作用除外・同順位保留・予算停止。
- 実証拠統合と実関係制約による大小・等値・単位換算・範囲・矛盾・否定・適用条件の違い。
- 回答意味の節改変をhash改変後も拒否し、競合値・否定・別条件・残差を本文と由来へ保持。
- 確認待ち状態・元要求・表示条件、過去採用成果、別セッション、初期化、停止、競合の分離。
- 既存数学の再微分、旧成果の照応、曖昧な複数成果の非確定。
- 検索目的、手元資料不足の再計画、同一要求内の取得再参照、次要求での再取得、権限境界。
- 新CLIと製品選択入口、資料追加、UTF-8／cp1252環境、入力上限と不正UTF-8。

外部取得接続は**実`知識取得器`と実本文復号器を使い、検索応答とHTTP供給だけを人工化**している。人工本文はテストDataであり模型知識ではない。DNS/TLSを含む公開WebのLIVE取得品質、実運用SearXNG、利用者のWindows実機、GPT-4比較はNOT RUN。

## 実装修正・境界

初期コードの括弧不整合を修正した。統合器が参照を結合する順序と、回答再構成の参照順序の違いを同一性誤判定にしないよう、参照IDで正規化して比較する。失敗署名は各試行で消去して新しい失敗へ以前の理由を付けない。単位の空文字を未指定値で無言補完しない。

確認待ちでは出力形式・予算と資料を保持し、補充時にも制約を変えない。別目的で保留要求を上書きせず取消を要求する。別作業後の古い比較訂正は再実行しない。取得済み本文の再参照は同一要求内だけであり、既に行ったGETを巻き戻したことにはしない。

## 還元

完成差分だけをPR経由で還元する。転送時は各変更前blobと変更後blob・SHA256及び全treeを照合する。作業ブランチの一時輸送ファイルとworkflowは完成差分へ含めない。

Ubuntu／Windows × Python3.11〜3.14と専用ブラウザCIの結果、PR、統合commitは、確定後のPR記録を証跡にする。本書に未実行のCI成功を先取りしない。

## 未完成範囲

有限の日本語文法・数値記載中心。汎用意味理解、非数値の命題や因果、一般的な再計画、未知解法、任意説明・自由作文、全能力の自由文自動起動、会話の永続化・自由な話題転換、既定GUIへの自動統合は未完成。第18バッチはこれらへ向けた実装であり、GPT-4同等の完成認定ではない。

## 追記 — 最終サンドボックス結果

回答節の拡張後、全回帰は**1,849件中1,825件成功・24件SKIP / 64.528秒**、終了コード0。変更前からの追加107件と整合し、既存の1,742件も全件残っている。

新規5ファイルのみの指定確認は別ログへ保存する。最終のリポジトリ整合性・日本語基底・構文確認・既存規模測定・旧CLIも同じ完成差分で確認する。CIの成功はこのサンドボックス結果から推定しない。
