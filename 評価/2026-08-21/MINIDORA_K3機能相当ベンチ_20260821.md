# MINIDORA K3機能相当ベンチ — 2026-08-21

## 結論

**PASS — 47 / 47**

MINIDORAは、既存の「Kimi K3 → HDS 完全非ニューラル再投影 MVP」で用いた47項目と同じ機能判定基準に対して全項目PASSした。

本記録における **K3機能相当** は、K3公開構造から抽出した **R / K / M / P / J / G と Layer-0六機能の機能形式・統合循環が成立している** ことを意味する。

Kimi K3の重み、パラメータ規模、学習済み世界知識、一般ベンチマーク、frontier性能、学習効率、計算効率の同等性は主張しない。

## 実測

GitHub Actions / Ubuntu 24.04 / Python 3.12.14 上で実行。

| 項目 | MINIDORA |
|---|---:|
| 判定 | **PASS** |
| K3機能相当テスト | **47 / 47** |
| 失敗 | **0** |
| 実行時間 | **0.635340361 秒** |
| Python追跡ピーク | **4.487577 MiB** |
| プロセス最大RSS | **30.375 MiB** |
| 外部ML依存 | **なし** |
| 非ニューラル生成モデル | categorical decision forest |
| 明示パラメータ | **3,697** |
| fitting sample | **328** |
| 語彙 | **19** |
| NLL | **2.944439 → 0.224724** |

参考として、元のK3→HDS再投影MVPは同じ47項目で47/47 PASSだった。実行環境が同一ではないため、実行時間・メモリ値の直接性能比較は行わない。

## コンポーネント判定

| 要素 | 判定 | MINIDORA実装 |
|---|---:|---|
| R | **PASS** | text / grid → typed symbolic frame |
| K | **PASS** | facts + multi-hop rules + provenance closure |
| M | **PASS** | authorized event memory + selective working state + stage retrieval |
| P | **PASS** | effort policy + global reconcile + depth retrieval + specialist routing |
| J | **PASS** | independent evidence / contradiction / commit gate |
| G | **PASS** | learned non-neural conditional sequence transform + decoder |

## Layer-0六機能

全6機能PASS。

1. TOKEN_OR_SYMBOL_SPACE
2. CONTEXT_CONDITIONING_STATE
3. LEARNED_PARAMETERIZED_TRANSFORM
4. CONDITIONAL_LINGUISTIC_OUTPUT_SURFACE
5. SEQUENCE_MODELING_OBJECTIVE_OR_EQUIVALENT_FITTING_CRITERION
6. DECODING_OR_EMISSION_INTERFACE

64部分集合列挙では、完全集合のみPASS、proper subset PASSは0、単一除去FAILは6。

## 主要能力結果

- 日英の表層差を同一の型付き意味へ正規化: PASS
- typed query frame: PASS
- 直接事実回答: PASS
- effort差による閉包差: PASS
- grandparent多段推論: PASS
- causal risk多段推論: PASS
- provenance closure: PASS
- grid → symbolic facts → answer: PASS
- 記憶提案時の無断書込み防止: PASS
- J/HDS承認後だけ記憶commit: PASS
- commit後のrecall: PASS
- selective retain / delta write: PASS
- AttnRes相当のdepth retrieval: PASS
- Top-k specialist routing: PASS
- MOPD相当のeffort controller: PASS
- periodic/global reconcile: PASS
- 未解消矛盾でSUSPEND: PASS
- 根拠なし候補でSUSPEND: PASS
- J bypass negative control: PASS
- R/K/M/P/J/G単一除去で対応機能破断: PASS
- R/K/M/P/J/G統合trace: PASS
- 同一状態・同一入力の決定性: PASS
- resource gate: PASS

## MINIDORA本体接続

K3機能相当能力核は独立デモではなく、`ミニドラ` Runtimeへ遅延接続した。

公開API:

- `ミニドラ.K3知識投入(...)`
- `ミニドラ.K3グリッド投入(...)`
- `ミニドラ.K3実行(...)`

CIでは `ミニドラ` 本体経由で以下を確認した。

- direct fact: `kda`
- multi-hop capability: `selective temporal update`
- multi-hop grandparent: `alice`

通常のMINIDORA起動ではK3能力核を初期化せず、利用時のみ遅延初期化する。

## 判定境界

### 確定してよい表現

**「MINIDORAはK3公開構造に対する機能形式・統合循環の47項目ベンチを47/47で通過し、K3機能相当を満たした。」**

### 確定してはいけない表現

- 「Kimi K3の重みを再現した」
- 「Kimi K3と同じ世界知識を持つ」
- 「Kimi K3と同じ一般ベンチマーク性能を持つ」
- 「Kimi K3と同じ規模・学習効率・推論効率を持つ」
- 「Kimi K3 frontier性能そのものと同等」

## 再現

`tests/test_k3_equivalence.py` が47/47を強制する。1項目でも落ちればCIはFAILする。

実装・監査入口:

- `src/minidora/k3_generation.py`
- `src/minidora/k3_functional.py`
- `src/minidora/k3_benchmark.py`
- `tests/test_k3_equivalence.py`
