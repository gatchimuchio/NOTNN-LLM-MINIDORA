# 第20バッチ — 命題候補・導出・説明の実装検証

日付: 2026-09-12。
基点commit: `bd414efcebd98936b5eca3acd84f63f770de2c69`。
基点tree: `e51b90c7a7254e35c15d9d8224932b64e7829d17`。

## 作業と境界

接続済みGitHubのmainを読み取りで確認し、添付の全追跡ソースと第17〜19バッチ差分・確定済み文書から復元した。復元した全Git treeが上記main内容と一致した。実パッケージと実公開HDSを利用する。疑似Compilerや疑似実行器で動作確認を代用しない。

作業はLinux / Python 3.13.5のサンドボックスで実装・検証し、2026-09-12にPR #94を経由してmainへ還元した。変更前の全回帰は独立した基点作業ツリーで実施した。変更後の確定結果とGitHub受入結果は末尾に分離して記録する。

## 実装

設計56を参照。原子・項・否定・連言・選言・含意・量化・時点・様相を保持し、有限導出の支持と反証を分離した。複数候補の問いは実行前に確認し、入力役割・命題解釈・判定・既存回答構成・採用・訂正・失効を接続した。

世界知識の辞書・正解表は追加していない。自然文資料に書かれた所属、関係、規則を処理する。条件導出を原因推定と同一視しない。

## 開発中の失敗と修正

- 最初の全基点回帰は外側の120秒上限で未完了。部分ログは保存し、成功へ算入しない。独立した記録runnerによる再実行は1,962件中1,938件成功・24件SKIP、終了コード0で完了した。
- 新規最初の88件では2件失敗。相反する時点や様相の入れ子を外側から上書きする実装を検出し、新規射影側で保持不能な入れ子を拒否するよう修正した。
- 会話接続を加えた131件では2件失敗。「この資料」の一意な参照束縛が実HDS照合へ届かない点と、JSON通信後の外側回答の再検証不成立を検出した。未知残差を一律に無視せず、実際に束縛した原文範囲だけを解消した。
- JSONの問題は変更前の数値回答でも再現した。PythonのtupleとJSONの配列の差で同値な記録を不一致としていたため、正規符号化でデータを比較するよう修正した。本文・値・出典・根拠の削除や改変は従来どおり拒否する。
- 修正後の命題141件は成功。実CLI・実localhost HTTP・デモを含む145件も成功した。独立真理値表との固定seed対照を含む。内部反復を独立試験件数として加算しない。

## 実際の接続

「すべての猫は哺乳類である」「太郎は猫である」から「太郎は哺乳類である」を導出した。根拠の再表示は、資料記載→全称具体化→条件適用の実依存から構成する。問いを鳥へ訂正すると、未確定を返し、偽と断じない。

否定記載を追加して同じ問いを実行すると、支持と反証の二経路を保持して矛盾を報告する。資料更新後は旧成果の再説明を停止する。

全称否定の二候補では、一個体の反例から「全員がそうとは限らない」は支持できても、「全員がそうではない」は確定できない差を実際に確認した。候補番号の選択で処理対象の式が変わる。番号選択前は実行器・採用状態を進めず、資料版が変わっていれば古い選択からの再開を止める。

複数資料・二項関係の変数束縛・時点/様相の分離、別の存在証人の非同一視、有限例からの全称化禁止、予算超過、停止、資料内命令の非実行、整合記録の改変検出も確認する。

## 残る境界

自然文は対応構文に限定。未知修飾、資料自身の曖昧さ、交互量化、一般の等号推論・選言除去・対偶・任意因果・自由作文は未完成。未導出は実装された導出方式で未確定という意味で、絶対的な導出不可能性の証明ではない。

報告の合格と問いの支持を区別する。資料の真実性・出典真正性やGPT-4同等性は認定しない。外部取得は今回使わず、公開Web LIVE品質とGPT-4比較は未実施。Windows・他Python版・実ChromiumはPR #94のCIで受入確認した。

## 確定したサンドボックス結果

環境: Linux / Python 3.13.5。変更後の全回帰実行中にソースは変更していない。

| 確認 | 結果 |
|---|---|
| 基点の独立全回帰 | 1,962件中1,938件成功、24件SKIP。67.946秒、終了コード0。 |
| 命題機能・接続の新規一括 | 145件成功、終了コード0。 |
| 変更後の全回帰 | 2,107件中2,083件成功、24件SKIP。70.582秒、終了コード0。 |
| リポジトリ整合性監査 | PASS。 |
| 日本語基底監査 | PASS。 |
| compileall | PASS。 |
| 既存CLI `python -m minidora "2+3"` | PASS。 |
| 既存規模測定 | PASS。既存回帰の維持であり今回の汎用性能評価ではない。 |
| 既存集合会話デモ | PASS。 |
| 命題会話デモ | 12発話の実行と判定・確認・訂正・失効が成功。 |
| 実製品CLI・監査台帳・localhost HTTP | 新規145件に含めてPASS。 |

24件SKIPは従来の任意ブラウザ関係。成功件数に含めない。独立した真理値表の対照は固定seed20の人工的な命題集合を使い、主張された支持/反証の健全性を調べるもので、実装した推論方式の完全性や任意自然文への性能を示すものではない。

ログ名: `baseline-first-timeout.log`、`baseline-final.log`、`new-first.log`〜`new-fourth.log`、`new-final.log`、`full-final.log`、`json-base-before.log`、`consistency.log`、`japanese.log`、`compileall.log`、`core-cli.log`、`scale.log`、`collection-demo.log`。終了コードとコマンドは同名のJSON/exit記録へ分離する。

## 配布差分の再適用確認

別のクリーンな基点作業ツリーへ、同梱予定の`適用.py`を使って検査・適用した。変更22ファイルの変更前後blob/SHA256を照合し、適用後の全Git treeがmanifestと一致した。利用者用index・HEAD・ブランチは変更しない。

再適用先の全回帰も、2,107件中2,083件成功・24件SKIP、68.514秒、終了コード0で完了した。元の作業ツリーと別の作業ツリーで、同じ完成実装を確認した。

適用スクリプトの既存変更・適用済み差分・配布ファイル改変・異なる基点の4条件では、それぞれ終了コード2で停止することを確認した。これらの配布操作確認は、命題機能145件や全回帰2,107件へ重複加算しない。

本節の検証書追記は全回帰完了後の文書変更であり、実装・試験ソースは変更していない。配布時には追記した文書も含む完成treeをmanifestへ固定し、最終差分をさらにクリーンな基点へ適用してtreeと実装ソースを再照合する。ハッシュは内容同一性を検査するもので、外部署名や真正性の証明ではない。

再適用ログ: `apply-check.log`、`apply-replay.log`、`apply-negative.json`、`reapply-full.log`と同名の終了記録。最終配布同一性は`package-final-check.json`へ分離する。

## GitHub受入・main還元

2026-09-12、検証済み差分を一時輸送workflow run `34647208810` で再構成した。圧縮差分SHA256・解凍差分SHA256・基点commit・完成treeを再検査し、最終作業branchのtreeが配布manifest固定値 `ce419bb70ee34b3724b258e7d1f97605c3e34886` と一致した。一時workflow・輸送ファイルはPR #94の差分へ含めていない。

- PR #94 `feat: 命題候補・導出説明を統合（第20バッチ）`。
- 再構築CI run `34647294734`: Ubuntu / Windows × Python 3.11–3.14 の全8構成でsuccess。各構成でリポジトリ整合性、日本語基底、構文、全単体試験、legacy規模測定、module / console CLI smokeを通過した。
- ブラウザCI run `34647294744`: Ubuntu / Windows の両構成でsuccess。実Chromiumの描画・ページ移動・遮断・HDS接続とローカル描画デモを通過した。
- main merge commit: `98613a235b37d65c7802f7510d0aa21915112de8`。merge treeは `ce419bb70ee34b3724b258e7d1f97605c3e34886` で、サンドボックスで検証した完成物と一致する。

還元時にこの対話環境で独立再実行した全回帰は外側待機上限で完走前に打ち切られたためPASSとは記録しない。サンドボックス作成時の完走記録は2,107件中2,083件成功・24件SKIPであり、還元時の全構成受入は上記GitHub CIで確認した。公開Web LIVE取得とGPT-4比較は引き続き未実施であり、本還元の成立証拠へ読み替えない。
