# MINIDORA Core37 正本固定監査 — 2026-09-08

## 結論

**判定: PASS — 現行Core正本として凍結・固定してよい。**

本監査は、MINIDORAの理論上限・将来性能上限・製品完成を認定するものではない。

固定する対象は、**現時点のLLM構成定義、既存LLM観測から得た構文化、MINIDORA Coreへの射影、HDS監督介入を含む現行実装**である。

この正本の意味は次である。

> **現行の射影・設計系列で採用可能な改善を反映し、固定条件のGPQA Diamond正式198問でCore 32/198、Core+HDS 37/198を確認した状態を、次回の再観測・再構文化・構成定義再監査までの基準点として保存する。**

37/198をMINIDORA方式そのものの理論限界とは扱わない。

---

## 1. 監査対象

実装対象commit:

```text
43360697098fe2a62df7ee077a14cefd851351db
```

対象commit以後に行う本監査文書・索引更新は文書固定であり、監査対象Core実装の性能変更を意味しない。

主要根拠:

- [`../docs/開発終了記録_2026-09-08.md`](../docs/開発終了記録_2026-09-08.md)
- 現行mainのCore / HDS / projection chain実装
- 固定C2 controlled replay結果
- Windowsローカル検証
- push後GitHub Actions

---

## 2. 固定性能

測定境界:

```text
benchmark     = GPQA Diamond 正式198問
OS            = Windows
Python        = 3.13.7
workers       = 4
choice seed   = 0
reference     = 固定参照コーパス C2
observer      = v4
external fetch during replay = なし
```

確認済み結果:

| 指標 | 固定比較基準 | 正本 | 差 |
|---|---:|---:|---:|
| Core正答 | 26 / 198 | **32 / 198 (16.16%)** | +6 |
| Core+HDS正答 | 32 / 198 | **37 / 198 (18.69%)** | +5 |
| Core 改善 / 退行 / 不変 | — | 8 / 2 / 188 | — |
| Core+HDS 改善 / 退行 / 不変 | — | 8 / 3 / 187 | — |

比較基準は `wh-controlled-replay-01/baseline` の述語探索短絡版である。clean mainとの別条件比較を、この固定C2の直接比較へ読み替えない。

正本側は質問IR・初期参照Rも変化しているため、得点差を純粋なCore内部推論だけへ帰属しない。検索への伝播を含む現行システム境界の結果として扱う。

---

## 3. 軽量監査

### 3.1 再現性

**PASS**

採用版の独立2回のC2再生で、次が一致している。

- 全396予測結果
- 実消費query / return
- HDS作用記録

これは固定された今回の再生境界における再現性証拠であり、未知入力・未知環境を含む普遍的決定論性の証明ではない。

### 3.2 回帰・実装整合性

**PASS**

Windowsローカル検証で以下を確認済み。

- `tools/Windows検証.ps1` 終了コード0
- 589単体試験 合格
- 依存整合性 合格
- リポジトリ整合性 合格
- 日本語基底確認 合格
- `compileall` 合格
- 規模測定回帰 合格
- Module / console CLI 合格
- 検証対象284ファイルが検証前後で不変
- 復元アプリ145ソース資産が採用凍結版manifestと一致

### 3.3 射影監査

**PASS**

現行projection chainでは、問いと匿名化した4候補を入力とし、正解ラベル・正解解説をCompilerへ渡さない境界へ修正済み。

確認済み:

```text
198 questions
792 candidates
人工射影契約 18 tests = PASS
```

この監査は、監査対象経路における契約成立を確認するものであり、あらゆる将来入力に対する完全性を意味しない。

### 3.4 push後Actions

**PASS**

実装対象commit `43360697098fe2a62df7ee077a14cefd851351db` に対して、取得時点で該当するGitHub Actions成功runは2件であり、失敗runを観測していない。

### 3.5 能力境界の分離

**PASS**

37/198は現行Core+HDS系列の正本値である。

2026-09-02の科学専門Capability Module controlled replayで得た63/198は、**Module OFF 8/198 → ON 63/198**という別の成立実証であり、Core単体性能へ混ぜない。

したがって次を維持する。

```text
Core正本性能
!= Module込みシステム能力
```

---

## 4. 既知の未達・限界

以下は未達のまま凍結する。

- GPQA Diamond 80/198 (40%) 未達
- 全面的な自然言語理解 未達
- 残差境界の完全解消 未達
- 記号関係候補は同一条件で性能低下し不採用
- 数量命題・数量証拠境界は未完成のため不採用

これらは、今回の正本採用を妨げる欠陥ではなく、**現行射影・設計系列の次回研究対象**として保持する。

---

## 5. 正本としての意味

この時点で追加の局所調整を正本更新条件にしない。

今回の結果から固定する境界は次である。

```text
現行LLM観測
  ↓
現行構文化
  ↓
現行LLM構成定義
  ↓
現行MINIDORA Core射影
  ↓
Core+HDS = 37 / 198
```

したがって37/198は、

- MINIDORA方式そのものの理論限界ではない
- GPQAで将来超えられない上限でもない
- 追加Module込みの上限でもない

一方で、

> **現行のLLM構成定義・構文化・Core射影・設計系列について、今回の改善探索を終了するための実測セーブポイントである。**

と扱う。

---

## 6. 再開条件

次に正本を置換する場合は、現在のCore内での追加チューニングだけを開始点にしない。

原則として次の系列から再開する。

```text
37で残った失敗・未表現領域の観測
  ↓
既存LLMの再観測
  ↓
能力成立作用の追加・再構文化
  ↓
LLM構成定義の再監査・必要なら更新
  ↓
MINIDORA Coreへ再射影
  ↓
同一または明示的に更新した評価境界で再測定
  ↓
現行正本37を置換できるか監査
```

新しい正本が成立するまでは、本書で固定するCore37系列を基準とする。

---

## 7. 最終判定

```text
実装整合性        PASS
固定条件再現性    PASS
射影契約          PASS
push後Actions     PASS
能力境界分離      PASS
未達事項の明示    PASS

現行Core正本採用  PASS
開発状態          FROZEN
```

**MINIDORA Core37を2026-09-08時点の凍結正本として固定する。**
