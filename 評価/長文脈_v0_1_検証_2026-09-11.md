# 長文脈管理 v0.1 — 第11バッチ検証記録

日付: 2026-09-11
基点commit: `ae3c828418795bd5e41b78a7801b2697ee6c6aa1`

## サンドボックス条件

Linux / Python 3.13.5。直接git接続はDNS解決失敗。接続済みGitHubでmain、AGENTS、Module境界を確認し、過去の配布ZIPからバッチ実装と試験を取得した。型・能力契約・要約・抽出・変換・検索の既存6ファイルは現行GitHub本文とGit blob SHA-1一致を確認した。

親パッケージをnamespace packageとして読み込む限定取得。全リポジトリのパッケージ初期化・全資産・実HDS Compiler接続は、この局所検証とは別である。既存実装の代用や擬似Coreを作っていない。

## 局所実測

| 対象 | 結果 |
|---|---|
| 原記録・依存・予算・改訂・保存復元・独立対照 | PASS / 56件 |
| 既存合成・抽出・独立CLI | PASS / 15件 |
| 今回の局所追加合計 | PASS / 71件 |
| 取得済み既存バッチ回帰 | PASS / 520件 |
| 同一プロセスの限定取得一括回帰 | PASS / 591件、20.816秒 |
| 実HDS Compiler・既存短期会話接続 | 局所ではNOT RUN / 6件を追加、完全checkoutのCIで確認 |
| 利用者Windows端末の実機試験 | NOT RUN |
| GPQA全数LIVE・GPT-4比較 | NOT RUN |

591件は第1・第4〜第10バッチの取得済み回帰と今回71件の合計。全リポジトリ試験数ではない。第2・第3バッチのHDS依存を含む試験と、新規HDS接続試験はサンドボックス一括対象から明示的に外し、CIの全回帰で検査する。

```bash
PYTHONPATH=src python -m unittest discover -s tests -p 'test_長文脈管理.py' -v
PYTHONPATH=src python -m unittest discover -s tests -p 'test_長文脈_合成.py' -v
python 証跡/run_local.py
python -m compileall -q src tests tools
python tools/長文脈デモ.py
```

`証跡/run_local.py`は配布ZIPの局所検証用で、リポジトリの標準全試験入口を変更しない。

## 作用差

101記録のうち古い原資料1件を選択し、既存の数値抽出Moduleで`120、75、45`を得た。デモの初回保存JSONは92149バイト、選択包は892バイト。これはUTF-8 JSONの実測であり、トークン数・処理速度・未知課題の精度ではない。残る100件の原文は破棄せず保存している。

復元後も同じ値を抽出。原資料を731へ明示改訂すると旧選択は拒否され、新しい資料から`731、75、45`が得られた。記録のない対照では後段の数値抽出は呼ばれない。

1024記録からの明示再参照、語一致による古い記録の優先、固定条件・残差の同伴、依存閉包を含む予算検査、旧成果の推移失効、失敗更新の原子性、競合更新、型・保存履歴・選択改変を検査した。

## 独立対照

4記録の前方依存6辺について全64構成を作り、各4始点からの選択を別の固定点反復で求めた依存閉包と照合。計256条件がすべて一致した。これは一つの単体試験内の反復で、試験件数へ水増ししない。一般的な意味依存の抽出を検査したものではない。

## 実装レビューによる境界修正

原資料の失効時に、そこへ依存する固定条件・残差まで現行候補から外れてしまう場合を整理した。依存失効した固定条件を未解決として選択保留にし、上位の明示的な退役・再登録を必要とする。旧版の条件を新しい版へ無言継承する処理も入れない。

起点のbool値を整数の世代・改訂へ等値扱いしないよう、公開更新境界で型を検査する。成果登録は選択の整合だけでなく資料化の成立も確認し、未成立の原資料を成功成果の入力として自動昇格しない。

## CIと還元

新規8ファイル、開発進捗1ファイル更新。既存Core、HDS Compiler、局所会話記憶、全既存Module、GPQA正本、標準チャット入口、GUI-Shellは変更しない。

Ubuntu / Windows × Python 3.11〜3.14の完全checkoutで全回帰と新規77件（局所71件＋実HDS6件）を確認する。未確認のCI結果を先取りしてPASSと記載しない。PRのrun・jobログ、還元コメントを別の確定証跡とする。

## 未完成範囲

上位が明示した原記録・依存・種別・改訂を対象とする初版。自由な長会話理解、意味的十分性、要約・巨大文書分割、トークン予算、ストレージ永続化・認証、短期会話との自動同期は未実装。固定条件を窓内に保つことと、下流がその意味に従って処理することは別である。

ハッシュや再選択は内部整合の検査であり、原資料の真正性や現実の事実性は保証しない。⑪全体・GPT-4同等性は未認定。
