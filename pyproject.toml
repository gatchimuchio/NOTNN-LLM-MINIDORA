[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "minidora-notnn"
version = "0.1.0"
description = "HDS→Layer-0→日本語命令P→外部参照Rで再構成する非ニューラルLLM研究実装"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
