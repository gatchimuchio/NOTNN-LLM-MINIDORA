# NOTNN-LLM-MINIDORA — ミニドラ

MINIDORAは、日本語を基底・規定・内部意味正本とする非ニューラルネットワーク型の言語模型研究実装である。

現行版は **v0.5.0**。

## 最上位理論と言語規定

本プロジェクトの最上位理論正本は [`cognitive-engineering-foundations`](https://github.com/gatchimuchio/cognitive-engineering-foundations) とする。

- 参照commit: `60131da52ba7931ed7f82c7648a74ac790f50d08`
- 基底言語・規定言語: 日本語

原則:

```text
日本語で対象化・差異化・関係化
→ 日本語で理論・設計・構文化・監査を成立
→ 日本語正本を保持
→ 実務上やむを得ない外部境界のみ多言語を例外使用
```

プログラミング言語、外部API、規格、既存固有名、URL、言語コード、国際公開等に必要な英字・外国語は外部互換として認めるが、第二基底・並列正本・内部意味の主語にはしない。

MINIDORA局所規定は [`設計/00_日本語基底規定_v1.md`](設計/00_日本語基底規定_v1.md) を参照する。

## 言語模型成立条件

言語模型として何が成立するかの責任正本は [`LLM-Constitutive-Specification`](https://github.com/gatchimuchio/LLM-Constitutive-Specification) とする。

- 版: `2026-08-28-成立規定-7`
- 参照commit: `debb83e091a705a5eac09ef4fb97a5b36305db6d`

認知工学最上位正本、言語模型成立規定、構文化、HDS Compiler、MINIDORA、実測は責任を分ける。ただし一方向の固定上流下流ではなく、新観測によって必要な前提まで再開放できる循環再帰関係として扱う。

## v0.5の核心

v7に合わせ、**厳密言語模型性と推論能力を分離**した。

```text
[厳密言語模型核]
完全言語状態空間
+ 持続模型状態
→ 厳密に正規化された条件分布
→ 連鎖則 + 終端
→ 完全系列確率

[能力模型核]
質問 / 候補 / 資料
→ 関係・証拠・候補差
→ 候補選択
```

既存の候補得点をsoftmax等で確率へ変換して言語模型確率と読み替える方式は採らない。

## 厳密言語模型核

`src/minidora/言語確率法則.py` に非ニューラル・決定論的な有限n-gram / finite-state成立形を実装する。`n-gram` 等は既存方式名として外部互換上保持する。

特徴:

- Python標準ライブラリのみ。
- `Fraction` による厳密有理数確率。
- NFKC Unicode文字 + `UNK/BOS/EOS`。
- 加算平滑化。
- 各接頭辞条件分布は厳密に1へ正規化。
- 系列確率は連鎖則と終端で計算。
- 全文脈で終端確率に正の下限を持ち、可変長の確率質量を閉じる。
- 模型状態をJSON互換辞書へ保存・復元可能。
- 同一形成資料なら順序に依存せず同じSHA-256。
- 無作為抽出なし。必要な選択は決定論的。

`最小厳密言語模型()` は世界知識を持たない `UNK/EOS` 事前状態で、**厳密言語模型法則の最小成立確認**だけを担う。能力やLargeを意味しない。

## 実行系

`ミニドラ()` は二核を別保持する。

```python
body.言語模型核   # MINIDORA厳密言語模型
body.能力模型核   # v0.4由来の候補・関係評価核
body.模型核       # 能力模型核の後方互換名
```

主要な公開呼出:

```python
body.言語確率("文章")
body.次記号分布("接頭辞")
body.言語模型監査()
body.言語評価("質問", ("候補A", "候補B"))  # 能力側
```

## 候補選択 / HDS

既存の候補選択経路は能力側として維持する。

```text
自然言語 / 資料
→ HDS Compiler
→ MINIDORA能力模型核
→ MINIDORA出力
→ HDS判断主体
  ├─ 合格 → 出力
  ├─ 保留 → 無出力
  └─ 拒否 → 無出力
```

外部互換上の元状態名 `APPROVE / HOLD / REJECT / OUTPUT / SILENT` は必要な実装境界に限って保持する。

HDS候補得点・参照寄与を厳密言語模型確率へ自動昇格しない。

## 計算経路

```text
日本語命令形P
→ 計算中間表現 v1
→ 計算実行境界 v1
→ 計算実行器
```

旧 `Layer0` は計算実行器の互換名であり、言語模型核ではない。

## 能力・Large・呼称

```text
厳密言語模型成立
!= 高推論能力
!= GPQA高得点
!= Large
!= 現代LLM呼称適合
```

`Large`、`LLM`、`GPQA` は外部で成立している名称なのでそのまま識別名として保持する。

v0.4の三面規模測定 `局所成立候補` は履歴として保持するが、v0.5へ自動継承しない。Largeはv7規模プロファイルで再監査対象とする。

## 日本語基底

現行共有言語基底はv0.4。

```text
規定言語 = 日本語
基底言語 = 日本語
基底言語コード = ja
```

`ja / en / zh` 等は入力・API等の外部互換識別コードであり、内部意味正本ではない。

外国語入力は必要部分を日本語の対象・関係・作用・状態へ射影してから内部計算へ渡す。

- [`設計/13_共有言語基底P仕様_v0_4.md`](設計/13_共有言語基底P仕様_v0_4.md)
- [`設計/14_外部言語_日本語意味射影仕様_v0_4.md`](設計/14_外部言語_日本語意味射影仕様_v0_4.md)

## 現行横断構文化

K3を除く10模型の現行横断観測は、

- [`構文化/言語模型横断_日本語基底作用構文化_v3/`](構文化/言語模型横断_日本語基底作用構文化_v3/)

とする。

直前 `構文化/LLM横断_状態差作用構文化_v2/` は、技術観測自体は保存するが外部原語を内部座標へ残した履歴として扱う。

## 現在の受入状態

```text
v0.5厳密言語模型核          = 合格
厳密正規化 / 終端           = 合格
模型状態保存・復元           = 合格
二核分離                     = 合格
v0.4候補選択回帰             = 合格
日本語基底規定               = 現行v0.4へ更新
GPQA / 推論能力              = 別評価
Large / 現代LLM呼称         = 再監査要
製品・最終完成               = 別関門
```

Linux / Windows × Python 3.11–3.14 のCIを正本更新ごとに確認する。過去の合格実測を新しいcommitへ無条件継承しない。

## 試験

```bash
python tools/repository_consistency_check.py
python tools/日本語基底監査.py
python -m compileall -q src tests tools
python -m unittest discover -s tests -v
python -m minidora "2+3"
```

CIはUbuntu / Windows × Python 3.11–3.14で確認する。

## 文書入口

- [`設計/README.md`](設計/README.md)
- [`設計/00_日本語基底規定_v1.md`](設計/00_日本語基底規定_v1.md)
- [`REFERENCES.md`](REFERENCES.md)
- [`構文化/README.md`](構文化/README.md)
- [`構文化/言語模型横断_日本語基底作用構文化_v3/README.md`](構文化/言語模型横断_日本語基底作用構文化_v3/README.md)
- [`評価/MINIDORA_v0_5_厳密LM受入_2026-08-28.md`](評価/MINIDORA_v0_5_厳密LM受入_2026-08-28.md)

## ライセンス

- **ソースコード、実行系、Compiler、ライブラリ、テスト、ツール、CI・パッケージ設定**: Apache License 2.0 (`Apache-2.0`)
- **仕様、設計、理論、論文、解説、図表、構文化・評価文書、README等**: Creative Commons Attribution 4.0 International (`CC-BY-4.0`)

これはデュアルライセンスではない。適用範囲は `LICENSE`、正式条件は `LICENSE-APACHE-2.0` / `LICENSE-CC-BY-4.0`、帰属は `NOTICE` を参照する。
