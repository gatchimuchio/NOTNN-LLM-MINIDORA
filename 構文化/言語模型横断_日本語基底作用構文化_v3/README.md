# 言語模型横断 日本語基底作用構文化 v3

- 実施日: 2026-08-28
- 対象: K3を除く10模型
- 規定言語: 日本語
- 状態: 現行横断構文化
- 旧版: `LLM横断_状態差作用構文化_v2` は観測履歴として保持

## 1. 再構文化理由

v2は技術観測の分別自体には有効だったが、内部座標に `runtime`、`harness`、`recurrent`、`branch` 等の外部原語を残し、英語圏の分類を日本語で説明する構造になっていた。

これは `cognitive-engineering-foundations` の日本語基底規定およびMINIDORA `00_日本語基底規定_v1` と一致しない。

v3では、外部原語を観測対象の固有名・出典照合用語へ限定し、内部構文化を日本語だけで成立させる。

## 2. 観測単位

- 状態担体 — 何が残るか
- 作用 — 何が状態へ作用するか
- 状態差 — 作用前後で何が変化するか
- 後続利用 — その差が後の計算へ使われるか
- 参照変更 — 状態によって参照重み・参照集合が変わるか
- 経路変更 — 状態によって実際の計算経路が変わるか
- 計算量変更 — 状態または入力条件で計算量が変わるか
- 再参照 — 過去状態・元資料を再び明示参照するか
- 再結合 — 複数状態・差・参照結果が再び合流するか
- 循環尺度 — 深さ、系列位置、対話履歴、外部実行、形成を分ける

## 3. 多言語例外

外部模型名、方式名、API固定名、URL、論文・公式資料の原語は実務上必要な例外として保存できる。

ただし外部原語から内部概念名を決めない。

例:

```text
外部原語: routing
内部正本: 経路選択 / 参照先選択 / 作用先選択
```

## 4. 機械可読資料

- `構文化規約_v3.json`
- `作用座標_v3.json`
- `観測台帳_v3/part_01.jsonl`
- `観測台帳_v3/part_02.jsonl`
- `構文化監査.py`

すべての内部鍵・区分値は日本語を正本とする。外部固有名は値として必要箇所だけ残す。
