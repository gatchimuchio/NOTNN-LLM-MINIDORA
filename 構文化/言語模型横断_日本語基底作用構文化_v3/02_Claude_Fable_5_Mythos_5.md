# Claude Fable 5 / Mythos 5 — 日本語基底作用構文化 v3

- K3: 対象外
- 観測深度: 公開境界中心

## 状態担体

模型中核内部状態は未観測。外部分類結果、外部記憶、実行条件を観測できる。

## 作用

外部分類器が入力を評価し、同一模型中核を使う経路または別処理への分岐を選ぶ。

## 状態差

分類結果によって後続の実行経路が変わる。これは模型中核内部の状態差ではない。

## 後続利用

外部記憶を再利用できる長期課題では、記憶状態の有無が後続成績へ差を生じさせる自然対照がある。

## 経路変更

外部分岐選択を直接観測できる。

## 循環尺度

外部記憶再利用・模型外分岐。模型中核内部循環は未観測。

## 未観測

- 模型中核内部構造
- 内部状態保持
- 内部仮説競合
- 内部経路選択

## 外部原語

`same underlying model`、`classifier`、`fallback`、`persistent file-based memory` は出典照合用の外部原語としてのみ保持する。
