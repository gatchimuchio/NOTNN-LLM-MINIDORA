# K2-V2 70B — 状態差作用構文化 v2

- 観測深度: D3 / dense long-context formation anchor
- K3: 対象外
- 状態: 再構文化。構成定義・MINIDORA仕様へ自動昇格しない。
- ライセンス: CC-BY-4.0

## 0. 根拠境界

旧v1 `10_K2-V2_70B.md`。Llama系に近いDense geometry、long-context/domain/reasoning/tool-use形成。

本v2では、公開資料が支持しない内部意味や循環を補わない。

## S — 状態担体

standard residual hidden state。

## A — 作用

global causal attention + Dense FFN + residual stack。

## Δ — 状態差

各layer変換差が後段stateへ累積。具体的意味は未観測。

## D — 後続依存

DEPTH_DOWNSTREAM_DEPENDENCYとstate-dependent attention weighting。

## P — 状態依存の経路変化

Dense path topology固定。expert routingなし。

## R — 再参照・再利用の尺度

各attention layerで広域context参照可能。明示runtime loopやcheckpoint reactivationは観測しない。

## C — 再結合

attention/FFN差がresidual主幹へ累積。

## F — 形成循環

Llama 3に近いgeometryへdomain/reasoning/long-context/tool-useを段階形成し、能力差が成立する自然実験。

## v1からの訂正・保持

v1の「80層に渡って再作用」を、fixed downstream transformationとして言い直す。形成済み関係の差とruntime再帰を混同しない。

## 未観測

weight意味、training raw data全量、long-context利用の意味分業。

## 判定

- `STATE_EXISTS` と `STATE_HAS_DOWNSTREAM_EFFECT` を分離する。
- `DOWNSTREAM_EFFECT` と `DYNAMIC_PATH_SELECTION` を分離する。
- `DEPTH_REUSE` / `POSITION_RECURRENCE` / `TURN_HISTORY` / `HARNESS_LOOP` / `FORMATION_LOOP` を同一の再帰へ潰さない。

## 一次資料再照合

現行公開資料との照合は `13_一次資料再照合.md` を参照。
