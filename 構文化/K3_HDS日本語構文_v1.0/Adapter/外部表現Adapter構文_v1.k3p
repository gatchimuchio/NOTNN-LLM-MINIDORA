# 外部表現Adapter構文 v1.0
# 内部日本語基底と外部言語・媒体は分離する。

Adapter {
  外部言語解析(入力言語, 記号列) -> 意味候補
  意味住所化(意味候補) -> 内部日本語意味
  視覚読取(画像|映像) -> 視覚表現
  視覚関係抽出(視覚表現) -> {空間関係, 時間関係, 文字, 対象}
  共通意味投影(視覚意味|言語意味) -> 内部意味
  外部表出(内部結果意味, 要求言語|媒体) -> 外部結果
}
