# 日本語基底正規化の移行履歴

PR #115 の一括置換器を、移行前HEAD `3099dfbb5377a99a504979c72245490201d1a47a` の内容のまま保存する。
拡張子 `.py.txt` は意図的なものであり、実行用ではない。

当時の置換は外部API固定名の誤変換、異なる内部概念の名称衝突、文字列参照の不一致、委譲先実装の欠落を生じさせた。
これらの修正後に旧置換器を再実行すると、修正を破壊し得る。現役の自動適用ワークフローと書換処理は退役させる。

現在の `tools/日本語基底正規化_実行.py` は読み取り専用の監査入口であり、置換器ではない。
新しい命名変更は、定義・参照・外部境界・試験を同一差分で修正し、現行の監査を実行する。
