# HDS全体運用v7：資料横断・対象接続の実装検証

2026-09-19（日本時間）。全能力開発ロードマップ順序2の部分増分。

## 取得元と作業場所

- 固定main：`574f22937b093985486d23c30c829d2b25cf7182`
- 固定tree：`b2611c318d151df03374e8d01a10fd7586a4e353`
- 既取得v5追跡ファイルとGitHubから取得した固定main差分をサンドボックスで再構成し、全tree一致を検査した。元のcommitオブジェクトも一致する。
- サンドボックスで設計・実装・試験を行う。転送用workflowと差分格納物は実装差分に含めない。
- 比較用に固定mainの独立作業木を保持し、新旧の全回帰を区別する。既存試験の失敗・中止を成功扱いしない。

## 実装範囲

明示的な資料横断依頼と、各資料冒頭の識別体系・時点・適用条件・対象識別子を接続する。対象の文字列一致ではなく、宣言された体系内の識別子で命題項を対応付ける。宣言の不一致、未宣言の対象、未解釈の宣言留保を接続から外し、理由と原文位置を保持する。

各資料の読解が実際に採用した記載だけを、出典付きの新しい記載IDで既存命題推論器へ渡す。隔離済み記載や宣言それ自体を事実にしない。引用・発言・量化内の同一性置換は本増分では行わず、時点・様相も書き換えない。原文、対象対応、適用範囲、実導出、実際の使用資料、不足、接続しなかった理由を保存し、説明へ渡す。

通常HDS四工程／明示不足調査六工程は維持し、新能力名や別の実行主体は追加しない。既存の六関係能力をv3へ更新する。公開取得資料は登録資料の接続群へ自動参加させない。横断判定で十分な場合だけ、許可済みの調査を実通信なしで終了できる。

保存版はv7。固定mainの実v6コードで保存・復元したfixtureから明示移行する。旧原本・履歴・依存・形成手順を保持し、旧成果を現行意味へ昇格させない。旧要求の通常復元拒否、再計算、設定権限の非継承を検査する。

## 新規試験

`test_HDS運用v7_資料横断.py` は35件。内訳は入口7件、宣言9件、導出・境界19件。1試験内で64通りの人工条件も照合する。

`test_HDS運用v7_運用保存.py` は18件。通常工程、保存・復元・再表現、ID・時点変更後の失効、無関係資料の追加、未確定時の説明、取消・文字上限、手順再利用、二段の読取許可、不要通信の省略、公開取得資料の非接続、保存改変検知、旧版移行を検査する。

合計53件。これは試験の個数であり、実行成功や汎用性能を意味する数ではない。64通りは53件の中に含まれる人工反例であり、追加の独立ベンチマークではない。期待する支持・反証をBoolean条件から別に計算し、同じ判定器を期待値生成に使用しない。

## 実装中の観測と訂正

初回の意味試験33件と64通りは成功した。通常経路の初回実演では、実演スクリプトが文章仕様に存在しない`単位`欄を参照してKeyErrorとなった。製品の内容構成は成功しており、スクリプトの参照先を既存仕様に訂正した。製品の仕様欄を追加する変更はしていない。

新規試験の初回一括実行は呼出しの時間上限で中断した。完走と数えない。別の実行で51件を完走し、1失敗・0エラーを観測した。工程試験が順序を持たない能力試行一覧を時系列として比較していたためで、実際のHDS作用履歴には正しい四工程順が記録されていた。能力集合とHDS作用履歴の時系列を分けて検査するよう試験を訂正し、当該1件は成功した。旧ログは残す。

その後、資料名の中に含まれる「を結び付けて」を横断命令と誤認しないよう、未完の横断指示検出を依頼構文の先頭へ限定した。従来入口の反例と、128記載の横断上限で切捨てや部分成功を返さない試験を追加した。修正後の意味試験35件は成功した。既存の推論器・通常HDS工程・旧版性能正本は変更していない。

修正済み作業木で追加分と全回帰を再実行する。最終実行結果、秒数、対象tree、PRのOS・Python別CIはPRと配布検証記録に実測として追記する。実行前に成功と記載しない。

## 再現コマンド

```sh
PYTHONPATH=src python -m unittest discover -s tests -p 'test_HDS運用v7_*.py' -v
PYTHONPATH=src python -m unittest discover -s tests -q
PYTHONPATH=src python tools/リポジトリ整合性監査.py
python tools/日本語基底監査.py
python tools/日本語基底詳細監査.py
python -m compileall -q src tests tools
```

試験投入前後の追跡ファイル指紋とGit treeを照合する。現行コードに合わせて旧試験を削除するのではなく、目録検査の既存集合・権限・重複禁止を維持したまま、運用版と関係能力の版だけを更新する。

## 未確認と残件

外部読取の接続試験は人工の検索・本文供給器を用いる。公開Webの実運用性能ではない。利用者Windows実機、開発から分離した一般未見課題、他モデルとの同条件比較は未確認。CIのWindows検証とは区別する。

識別宣言は入力に明示された前提であり、対象同一性の世界事実を保証しない。任意文章からの識別体系抽出、一般動詞・修飾、異なる時点・条件の適合判断、曖昧な対象照応、一般的な資料信頼性・最新性判定、広域知識供給、自由な内容生成、後続開発順序は台帳に残す。
