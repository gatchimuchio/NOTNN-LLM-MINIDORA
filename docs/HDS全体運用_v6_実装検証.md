# HDS全体運用v6：実装・検証記録

2026-09-18 / 基点main `49949048d6c20d991caf81025f97b9204505907c`  
基点tree `38caf0aed103d51a567707fdd0bf64eead8a4691`

## 取得と実装場所

GitHub接続で現行mainを確認した。通信制約により通常のgit取得ができないため、GitHub Actionsに保存された固定v4ソースと、v4から統合済みv5への差分をサンドボックスへ取得した。v4の全追跡ファイルから作ったGit treeとv4正本treeの一致、差分適用後の全treeと今回確認したmain treeの一致を検査してから作業した。古いZIPをmainと推定したものではない。

v4の取得物はrun35289927966、artifact10525568174。中のsource.tar.gzのSHA256は`ecb49d849417e31af13003124de11d358a835b880dea06824ec097a50c235ff4`。v5基点の追跡ファイルは1,145件。変更前全回帰用とv6編集用の作業木を分離し、基準測定中のv5ファイルは変更していない。

前回未完了だったv5のmain CIは、今回run35298171813がsuccessで終了したことを確認した。v6検証の成功とは読み替えない。

## 実装増分と境界

格役割を保持する動作、34通常動詞とサ変の対応活用、必要／十分条件の方向、作用域を明示した例外を実導出と内容構成へ接続した。既存の関係六能力の契約をv2へ更新し、通常4工程／不足取得6工程、既存命題推論、生成、原記録、純粋手順形成を使用する。新しい能力名や運用主体は作らない。

例外の不成立を通常記載から独立に支持でき、採用後に自己・相互阻害がない規則だけを採用する。原文・未採用理由・撤回時の実導出も保存する。任意の修飾や例外作用域の理解ではない。

保存版をv6へ更新し、実v5保存物の明示移行を追加した。旧関係読解v1は再検査用に保持し、明示再計算だけを現行v2へ接続する。旧原本と入れ子の移行履歴を保持し、旧成果・旧形成手順を未検証のまま再採用しない。

## 開発中の不具合・試験訂正

失敗を含む実行ログは配布検証一式に保持する。以下は当時の観測であり、後の成功で無言上書きしない。

- 意味試験の初回53件では、同じ試験の二つの入力で失敗した。「書類をすぐに読む」の「すぐ」を格項、「書類を読んで送る」の「読ん」を格項として受理していた。新しい格項を対応する単純名詞へ限定し、修飾・接続を主体や目的物へ押し込まないよう修正した。これはひらがなを含む任意名称の一般分割を未対応として残す変更である。修正後53件は成功。
- v5追加分の初回92件では1件失敗。新読解が未束縛参照を拒否していたが、残差理由が一般的な「未確定」に変わっていた。未束縛という識別可能な失敗理由を保持するよう修正した。再実行92件は失敗0・エラー0・skip0、214.310秒。
- v6意味・運用・移行の最初の統合77件は2件失敗。既存の`関係回答を検査`は不整合時にFalseを返すが、新テストが例外送出を期待していた。製品の検査を弱めず、Falseの検査と外側再封緘後の復元拒否へ訂正した。
- 追加境界試験を含む59件では1件失敗。「が正常ではない」の否定述語内の「は」が第二主格と判定され、読解できなかった。v5再現コードは変更せず、v6の明示名詞否定に主格接続を追加した。60件の再実行は失敗0・エラー0・skip0。

## 検証対象と再現入口

新規試験は意味60件、移行11件、運用接続13件の計84件。既存3,491件と合わせて3,575件を全回帰の対象とする。これらは試験件数であって、最終実行前の成功を表す値ではない。既存試験は削除せず、能力目録の二つの現行版期待だけを更新した。旧能力・版・権限・重複の検査は保持する。

```sh
PYTHONPATH=src python -m unittest discover -s tests -p 'test_HDS運用v6_*.py' -v
PYTHONPATH=src python -m unittest discover -s tests -p 'test_HDS運用v5_*.py' -v
PYTHONPATH=src python -m unittest discover -s tests -q
PYTHONPATH=src python tools/リポジトリ整合性監査.py
python tools/日本語基底監査.py
python tools/日本語基底詳細監査.py
python -m compileall -q src tests tools
PYTHONPATH=src python -m minidora '2+3'
```

静的整合性、日本語基底、詳細監査、構文確認はサンドボックスで成功を観測済み。最終全回帰は投入時の追跡ファイル指紋とtreeを固定し、試験後に不変を検査する。OS別CIは既存のUbuntu／Windows×Python3.11〜3.14を使い、既存workflowは変更しない。最終回帰結果、ブランチhead、検証対象tree、PR CI、main統合commitは本差分のPRと配布検証記録で照合する。マージ前とマージ後のCIを区別する。

## 保存fixtureの独立性

`tests/資料/HDS旧保存/v5.json.gz`は固定mainの未変更コードで、関係回答と純粋形成手順を生成して保存・復元したもの。`v5_移行済.json.gz`は実v4移行済み保存物から同じv5コードで移行したもので、v3→v4→v5の原本を含む。

新コードの版ラベルだけを旧版へ書換えたものではない。由来、元目録、原本と圧縮物の実SHA256は`由来_v5.json`に保持する。履歴の自己参照・未来版、由来や目録の改変、保存からの権限獲得を拒否する。旧意味結果が旧読解で一致することと、明示再計算で新意味が実際に使われることを別々に検査する。

## 性能と未確認

外部読取の試験は人工の検索・本文供給器を既存取得器へ接続したもので、公開Web実運用ではない。語順や否定の人工反例、未知の対象名を合成する試験は、一般的な未見依頼の性能測定ではない。

利用者Windows実機、公開Web総合運用、開発と分離した汎用未見課題、GPT-4等との同条件比較は未確認。順序2全体と後続残件は未完了。新しい語彙数・機能数・CI成功から汎用能力全体の成立を導かない。
