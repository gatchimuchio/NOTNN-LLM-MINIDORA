# 日本語基底整合性置換の統合記録

## 修復済み成果

- 対象: PR #115。
- 修復反映commit: `b9f180964cffb39959856b75138c417916cf66db`。
- 検証対象tree: `ff063d6ea48ab997d567dde3352726e59e8ae9c8`。
- 修復内容と修復前後の比較: [修復記録](日本語基底整合性置換_検証記録_2026-09-16.md)。

## GitHub上で確認した実行結果

実行日時: 2026-09-16 18:18–18:20（日本時間）。環境: Ubuntu / Python 3.12.14。

実行記録: https://github.com/gatchimuchio/NOTNN-LLM-MINIDORA/actions/runs/35061907907/job/104737142640

| 検証 | 結果 |
| --- | --- |
| 適用前後の内容ハッシュと完成tree照合 | 合格 |
| パッケージ導入 | 合格 |
| 日本語基底監査・日本語基底詳細監査 | 両方合格 |
| 構文化v3監査・リポジトリ整合性監査 | 両方合格 |
| `src / tests / tools` の構文確認 | 合格 |
| 単体試験 | 2,963件、失敗0・エラー0・スキップ24 |
| 規模測定の履歴回帰 | 合格、共有適用256/256 |
| モジュール起動・コンソール起動 | 両方で `2+3` に `5です。` |
| 検証後の完成tree再照合 | 合格、上記treeと一致 |

2,963件は発見された試験総数であり、実行合格は2,939件。スキップ24件を合格に算入しない。
固定修復ジョブはその後、同一作業ブランチへ修復成果を反映して正常終了した。

## mainとの統合方針

統合元main: `a68faaf2832a3cf060273cc072acc90165bf0887`。
PRの従来基点 `0491debf2362843c22789d79ff670df1eddd19be` からのmain差分は、次の一時ワークフロー3経路だけであることを再確認した。

- `日本語基底正規化_一時適用.yml`: mainで削除済み。復活させない。
- `日本語基底正規化_一時検証.yml`: 固定作業ブランチ専用。通常CIへ引き継ぐため完成側では保持しない。
- `日本語基底正規化_正規化前取得.yml`: 移行前tree取得専用。役目を終えたため完成側では保持しない。

両方の履歴を親として保持し、修復済みの実装・試験・監査器・通常CIには追加の一括置換を行わない。取得済み履歴資産は削除しない。

## この記録時点で未確定の範囲

Windows / Ubuntu × Python 3.11–3.14の通常CI、および実Chromiumを用いるブラウザ別構成は、この統合差分に対する結果を別途確認する。
PR自動検証の `action_required` は合格ではなく、試験未実施として扱う。
承認設定・ブランチ保護・試験条件は緩和しない。新たなGPQAのLIVE性能測定はこの置換作業に含めない。
