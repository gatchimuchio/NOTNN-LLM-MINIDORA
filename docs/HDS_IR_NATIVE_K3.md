# HDS-IR Native K3 Path

HDS Compilerが形成した一般HDS-IRを、表層自然言語へ戻さずK3相当能力核へ直接渡す接続層。

- ベンチ名・設問固有語・正解情報に依存しない。
- `choice:*` 座標から候補集合を受け取る。
- Kの根拠事実が候補と問いの双方に接続するときだけ候補を形成する。
- 根拠なし・同率根拠はJ/HDSがSUSPENDし、推測で回答しない。
- HDS Compiler内部は本公開リポジトリへ含めない。
