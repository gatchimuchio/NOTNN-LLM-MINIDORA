# HDS全体運用v5：実装・検証記録

2026-09-18 / 基点main `09043dc222ecda52996e9dc0e53ad9c5a809c27e`

## 取得と作業場所

GitHubの固定mainを取得し、サンドボックスで編集・試験した。取得時のtreeは`c31d151c337b9d74642481c9e515ce72fddcf845`。取得した全追跡ファイルからサンドボックスで作成したGit treeが一致した。取得用の一時workflowはmainへ統合しない。

基準mainの変更前回帰は3,399件、失敗0・エラー0、skip24件、760.832秒。旧v4の性能値や過去の試験結果は変更しない。

## 実装差分

関係質問、有限な名詞述語・サ変活用・条件・局所照応、実際の命題導出、未確認前件候補、不足取得後の再読解、実根拠からの内容構成を接続した。通常は4工程、明示不足調査では6工程を既存HDSが実行する。新規は6能力であり、新しい制御主体ではない。

原文・区間・変換・実導出・残差を保存し、生成文章を再構成して照合する。資料更新、全資料集合の変更、再表現、保存復元、純粋手順形成へ接続した。標準v4の実保存物とv3→v4移行済み実保存物からのv5移行も追加した。

## 検証対象

- 意味反例：条件と前件、後件肯定、前件否定、否定と矛盾、時点・様相・発言、進行・過去・能力、照応の曖昧性、例外隔離、原文・構造改変、未知名称と語順。
- 通常入口：4／6工程、既存生成、資料不足、文字予算、取消、二段の外部許可、取得の要否、取得後も意味不十分な資料、取得失敗、保存・再表現時の非通信、原文更新・全資料追加による失効、純粋手順形成。
- 保存移行：固定v4原本のハッシュ、元の履歴・依存・形成手順保持、移行済み履歴保持、通常復元での旧版拒否、改変・独自目録・権限取得の拒否、明示再計算。

外部読取試験は既存取得器に人工の検索・本文供給器を接続した。検索スニペットと取得本文を意図的に変えて、本文が使われることを確認する。公開Webの実サービス測定とは呼ばない。

## 開発中に検出した不具合

初回の意味試験42件では、失敗1・エラー4を観測した。空の採用記載を既存推論器へ渡したエラーは、偽の事実を足さず「推論未実施・未確定」を返すよう修正した。文をまたぐ無標識の省略を誤って補った失敗は、同一文内だけの省略と、文間の明示指示語を区別して修正した。否定文の「ではない」の助詞で主体を見失う場合と、未束縛指示語を定数にする危険も修正・反例追加した。

追加75件の初回実行では、1件の検査失敗を観測した。更新後の文章全体に「条件適用」が存在しないという試験が、境界を説明する注意書きまで旧導出と誤判定していた。注意書きや実装を削らず、保存された実導出に旧条件適用がないことと、旧有効条件の本文がないことを検査するよう修正した。修正前の実行記録を保持し、読込済みの旧試験を含む全回帰を中止して、修正後の全回帰を再実行する。

## 検証の再現

```sh
PYTHONPATH=src python -m unittest discover -s tests -p 'test_HDS運用v5_*.py' -v
PYTHONPATH=src python -m unittest discover -s tests -q
PYTHONPATH=src python tools/リポジトリ整合性監査.py
python tools/日本語基底監査.py
python tools/日本語基底詳細監査.py
python -m compileall -q src tests tools
```

新規試験は意味48件・運用接続18件・保存移行9件の75件。変更前の3,399件を削除・変更せず、合計3,474件を検証対象とする。これは試験の件数であり、実行成功を意味する数字ではない。

取得元、試験投入時のソース指紋、成功・失敗・中止を含む実行ログ、最終tree一致、PRのOS別CIを区別して記録する。最終実行結果と統合先commitは本差分のPRおよび配布検証ログで照合する。静的監査と構文・起動確認はサンドボックスで成功を観測済み。

## 未確認・残件

利用者のWindows実機、公開Web実運用、開発から分離した汎用未見依頼の性能、GPT-4等との同条件比較は未確認。CI成功や人工入力の意味反例を汎用性能へ読み替えない。順序2も未完了で、一般動詞・条件例外・対象同一性・広域知識・一般内容生成の残差を台帳に保持する。
