from __future__ import annotations

# v0.3までLayer0と呼んでいた実体は、LLM成立構造ではなく汎用命令実行器だった。
# 実装本体は履歴互換の ``runtime_v03`` と同様に旧実装を保持し、この公開名で
# 役割だけを正す。
from .旧_layer0_v03 import Layer0 as _旧Layer0, 実行文脈


class 計算実行器(_旧Layer0):
    """日本語命令形Pを決定論的に実行する汎用計算器。

    大規模言語模型の模型中核ではない。算術、比較、状態更新等の計算作用を
    実行する運用部品であり、言語模型性は ``模型.MINIDORA模型核`` が担う。
    """


__all__ = ["計算実行器", "実行文脈"]
