"""MINIDORA計算行為核の共通契約。

ここは入力表現器や専門解法を所有しない。対象束縛、条件操作、関係導出、
内容計画、検証管理、能力境界、実行内効果の共通責任だけを公開する。
"""
from .責任境界 import コア責任契約, コア責任, コア責任を取得, コア責任集合を検査, コア責任閉包を検査
from .意味操作 import 束縛を試す, 具体化する, 意味座標
from .条件操作 import 条件判定, 条件状態, 条件項, 条件集合を判定
from .関係操作 import 前方閉包, 条件付き前方閉包
from .内容計画 import 内容単位, 内容計画, 内容計画を構成, 内容計画を検査, 内容計画を表現
from .検証管理 import 検証器群を実行
from .能力境界 import 能力境界契約, 標準能力境界, 能力境界を取得, 標準能力境界を照合, 標準能力登録を閉包, 能力分類集計, 責任境界集計
from .効果 import 期待効果
from .状態操作 import 状態ビュー, 状態を読む, 状態差を受理, 閉包を確認, ノード意味署名, ノード意味値

__all__ = [
    "コア責任契約", "コア責任", "コア責任を取得", "コア責任集合を検査", "コア責任閉包を検査",
    "束縛を試す", "具体化する", "意味座標",
    "条件判定", "条件状態", "条件項", "条件集合を判定",
    "前方閉包", "条件付き前方閉包", "内容単位", "内容計画", "内容計画を構成", "内容計画を検査", "内容計画を表現",
    "検証器群を実行",
    "能力境界契約", "標準能力境界", "能力境界を取得", "標準能力境界を照合", "標準能力登録を閉包", "能力分類集計", "責任境界集計",
    "期待効果", "状態ビュー", "状態を読む", "状態差を受理", "閉包を確認", "ノード意味署名", "ノード意味値",
]
